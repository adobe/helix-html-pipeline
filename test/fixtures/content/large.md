+-----------------------------------------------------------------------------------------------------------------------+
| Breadcrumbs                                                                                                           |
+=======================================================================================================================+
| [설명서](/en/docs/)[Commerce](/ko/docs/commerce)[명령줄 도구 참조](/ko/docs/commerce-operations/reference/commerce-on-premises) |
+-----------------------------------------------------------------------------------------------------------------------+

# bin/magento(Magento Open Source)

+------------------------------------------------------------------------------+
| Article Metadata                                                             |
+==============================================================================+
| Last update: Tue Jun 27 2023 00:00:00 GMT\+0000 (Coordinated Universal Time) |
+------------------------------------------------------------------------------+

+-------------------------+
| Article Metadata Topics |
+=========================+
| - 주제:                   |
| - [Configuration](#)    |
+-------------------------+

+----------------------------+
| Article Metadata Createdby |
+============================+
| 작성 대상:                     |
|                            |
| - Experienced              |
| - Admin                    |
| - Developer                |
+----------------------------+

<!-- All the assigned and captured content is used in the included template -->

<!-- The template to render with above values -->

**버전**: 2.4.6

이 참조는 다음을 통해 사용할 수 있는 114개의 명령을 포함합니다. `bin/magento` 명령줄 도구입니다.\
초기 목록은 다음을 사용하여 자동으로 생성됩니다. `bin/magento list` Magento Open Source 시 명령.\
사용 ["CLI 명령 추가"](https://developer.adobe.com/commerce/php/development/cli-commands/) 사용자 지정 CLI 명령을 추가하는 방법 안내서를 참조하십시오.

+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| Note (note)                                                                                                                                                                                                                                                                             |
+=========================================================================================================================================================================================================================================================================================+
| :info: NOTE                                                                                                                                                                                                                                                                             |
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| 다음을 호출할 수 있습니다. `bin/magento` 전체 명령 이름 대신 바로 가기를 사용하는 CLI 명령. 예를 들어 `bin/magento setup:upgrade` 사용 `bin/magento s:up`, `bin/magento s:upg`. 다음을 참조하십시오 [바로 가기 구문](https://symfony.com/doc/current/components/console/usage.html#shortcut-syntax) CLI 명령을 사용하여 바로 가기를 사용하는 방법에 대해 알아봅니다. |
+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| Note (note)                                                                                                                                                                                                                                                                                 |
+=============================================================================================================================================================================================================================================================================================+
| :info: NOTE                                                                                                                                                                                                                                                                                 |
+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| 이 참조는 응용 프로그램 코드베이스에서 생성됩니다. 콘텐츠를 변경하기 위해에서 해당 명령 구현에 대한 소스 코드를 업데이트할 수 있습니다 [코드베이스](https://github.com/magento) 검토를 위해 변경 사항을 보관하고 제출합니다. 다른 방법은 _피드백 제공_ (오른쪽 상단에서 링크를 찾습니다.). 기여도 가이드라인은 를 참조하십시오. [코드 기여](https://developer.adobe.com/commerce/contributor/guides/code-contributions/). |
+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

## `_complete`:anchor--1:

셸 완료 제안을 제공하는 내부 명령

+---------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                          |
+===============================================================================================================+
| ```                                                                                                           |
| bin/magento _complete [-s\|--shell SHELL] [-i\|--input INPUT] [-c\|--current CURRENT] [-S\|--symfony SYMFONY] |
| ```                                                                                                           |
+---------------------------------------------------------------------------------------------------------------+

### `--shell`, `-s`:anchor--2:

셸 유형("bash")

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--input`, `-i`:anchor--3:

입력 토큰의 배열(예: COMP\_WORDS 또는 argv)

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
| - 값 필요      |
+-------------+

### `--current`, `-c`:anchor--4:

커서가 있는 "입력" 배열의 인덱스(예: COMP\_CWORD)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--symfony`, `-S`:anchor--5:

완료 스크립트의 버전

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--6:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--7:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--8:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--9:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--10:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--11:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--12:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `completion`:anchor--13:

셸 완료 스크립트 덤프

+-------------------------------------------------+
| Code (language bash)                            |
+=================================================+
| ```                                             |
| bin/magento completion [--debug] [--] [<shell>] |
| ```                                             |
+-------------------------------------------------+

### `shell`:anchor--14:

셸 유형(예: "bash"), "$SHELL" 환경 변수 값이 제공되지 않으면 사용됩니다.

### `--debug`:anchor--15:

완료 디버그 로그 추적

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--16:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--17:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--18:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--19:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--20:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--21:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--22:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `help`:anchor--23:

명령에 대한 도움말 표시

+------------------------------------------------------------------+
| Code (language bash)                                             |
+==================================================================+
| ```                                                              |
| bin/magento help [--format FORMAT] [--raw] [--] [<command_name>] |
| ```                                                              |
+------------------------------------------------------------------+

### `command_name`:anchor--24:

명령 이름

+---------------+
| List (ol)     |
+===============+
| - 기본값: `help` |
+---------------+

### `--format`:anchor--25:

출력 형식(txt, xml, json 또는 md)

+--------------+
| List (ol)    |
+==============+
| - 기본값: `txt` |
| - 값 필요       |
+--------------+

### `--raw`:anchor--26:

원시 명령 도움말을 출력하려면

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--27:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--28:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--29:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--30:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--31:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--32:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--33:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `list`:anchor--34:

목록 명령

+-------------------------------------------------------------------------+
| Code (language bash)                                                    |
+=========================================================================+
| ```                                                                     |
| bin/magento list [--raw] [--format FORMAT] [--short] [--] [<namespace>] |
| ```                                                                     |
+-------------------------------------------------------------------------+

### `namespace`:anchor--35:

네임스페이스 이름

### `--raw`:anchor--36:

원시 명령 목록을 출력하려면

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--format`:anchor--37:

출력 형식(txt, xml, json 또는 md)

+--------------+
| List (ol)    |
+==============+
| - 기본값: `txt` |
| - 값 필요       |
+--------------+

### `--short`:anchor--38:

명령의 인수 설명을 건너뛰려면

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--39:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--40:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--41:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--42:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--43:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--44:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--45:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `admin:adobe-ims:disable`:anchor--46:

Adobe IMS 모듈 비활성화

+-------------------------------------+
| Code (language bash)                |
+=====================================+
| ```                                 |
| bin/magento admin:adobe-ims:disable |
| ```                                 |
+-------------------------------------+

### `--help`, `-h`:anchor--47:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--48:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--49:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--50:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--51:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--52:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--53:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `admin:adobe-ims:enable`:anchor--54:

Adobe IMS 모듈을 활성화합니다.

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento admin:adobe-ims:enable [-o\|--organization-id [ORGANIZATION-ID]] [-c\|--client-id [CLIENT-ID]] [-s\|--client-secret [CLIENT-SECRET]] [-t\|- |
| -2fa [2FA]]                                                                                                                                              |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--organization-id`, `-o`:anchor--55:

Adobe IMS 구성에 대한 조직 ID를 설정합니다. 모듈을 활성화할 때 필요합니다.

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--client-id`, `-c`:anchor--56:

Adobe IMS 구성에 대한 클라이언트 ID를 설정합니다. 모듈을 활성화할 때 필요합니다.

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--client-secret`, `-s`:anchor--57:

Adobe IMS 구성을 위한 클라이언트 암호 를 설정합니다. 모듈을 활성화할 때 필요합니다.

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--2fa`, `-t`:anchor--58:

Adobe Admin Console의 조직에 대해 2FA가 활성화되어 있는지 확인합니다. 모듈을 활성화할 때 필요합니다.

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--help`, `-h`:anchor--59:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--60:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--61:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--62:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--63:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--64:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--65:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `admin:adobe-ims:info`:anchor--66:

Adobe IMS 모듈 구성 정보

+----------------------------------+
| Code (language bash)             |
+==================================+
| ```                              |
| bin/magento admin:adobe-ims:info |
| ```                              |
+----------------------------------+

### `--help`, `-h`:anchor--67:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--68:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--69:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--70:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--71:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--72:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--73:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `admin:adobe-ims:status`:anchor--74:

Adobe IMS 모듈 상태

+------------------------------------+
| Code (language bash)               |
+====================================+
| ```                                |
| bin/magento admin:adobe-ims:status |
| ```                                |
+------------------------------------+

### `--help`, `-h`:anchor--75:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--76:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--77:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--78:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--79:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--80:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--81:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `admin:user:create`:anchor--82:

관리자 만들기

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento admin:user:create [--admin-user ADMIN-USER] [--admin-password ADMIN-PASSWORD] [--admin-email ADMIN-EMAIL] [--admin-firstname ADMIN-FIRSTNAM |
| E] [--admin-lastname ADMIN-LASTNAME] [--magento-init-params MAGENTO-INIT-PARAMS]                                                                         |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--admin-user`:anchor--83:

(필수) 관리자

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--admin-password`:anchor--84:

(필수) 관리자 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--admin-email`:anchor--85:

(필수) 관리자 전자 메일

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--admin-firstname`:anchor--86:

(필수) 관리자 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--admin-lastname`:anchor--87:

(필수) 관리자 성

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--magento-init-params`:anchor--88:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--89:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--90:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--91:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--92:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--93:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--94:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--95:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `admin:user:unlock`:anchor--96:

관리자 계정 잠금 해제

+------------------------------------------+
| Code (language bash)                     |
+==========================================+
| ```                                      |
| bin/magento admin:user:unlock <username> |
| ```                                      |
+------------------------------------------+

### `username`:anchor--97:

잠금 해제할 관리자 사용자 이름

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--help`, `-h`:anchor--98:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--99:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--100:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--101:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--102:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--103:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--104:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `app:config:dump`:anchor--105:

애플리케이션 덤프 만들기

+-------------------------------------------------+
| Code (language bash)                            |
+=================================================+
| ```                                             |
| bin/magento app:config:dump [<config-types>...] |
| ```                                             |
+-------------------------------------------------+

### `config-types`:anchor--106:

공백으로 구분된 구성 형식 목록 또는 모두 덤프하지 않음 \[범위, 시스템, 테마, i18n]

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--107:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--108:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--109:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--110:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--111:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--112:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--113:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `app:config:import`:anchor--114:

공유 구성 파일에서 적절한 데이터 저장소로 데이터 가져오기

+-------------------------------+
| Code (language bash)          |
+===============================+
| ```                           |
| bin/magento app:config:import |
| ```                           |
+-------------------------------+

### `--help`, `-h`:anchor--115:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--116:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--117:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--118:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--119:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--120:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--121:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `app:config:status`:anchor--122:

구성 전파에 업데이트가 필요한지 확인합니다.

+-------------------------------+
| Code (language bash)          |
+===============================+
| ```                           |
| bin/magento app:config:status |
| ```                           |
+-------------------------------+

### `--help`, `-h`:anchor--123:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--124:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--125:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--126:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--127:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--128:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--129:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `braintree:migrate`:anchor--130:

Magento 1 데이터베이스에서 저장된 카드 마이그레이션

+-----------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                      |
+===========================================================================================================+
| ```                                                                                                       |
| bin/magento braintree:migrate [--host HOST] [--dbname DBNAME] [--username USERNAME] [--password PASSWORD] |
| ```                                                                                                       |
+-----------------------------------------------------------------------------------------------------------+

### `--host`:anchor--131:

호스트 이름/IP. 포트는 선택 사항입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--dbname`:anchor--132:

데이터베이스 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--username`:anchor--133:

데이터베이스 사용자 이름. 읽기 액세스 권한이 있어야 합니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--password`:anchor--134:

암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--135:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--136:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--137:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--138:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--139:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--140:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--141:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cache:clean`:anchor--142:

캐시 유형을 지웁니다.

+-------------------------------------------------------------------+
| Code (language bash)                                              |
+===================================================================+
| ```                                                               |
| bin/magento cache:clean [--bootstrap BOOTSTRAP] [--] [<types>...] |
| ```                                                               |
+-------------------------------------------------------------------+

### `types`:anchor--143:

공백으로 구분된 캐시 유형 목록 또는 모든 캐시 유형에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--bootstrap`:anchor--144:

부트스트랩의 매개 변수 추가 또는 재정의

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--145:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--146:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--147:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--148:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--149:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--150:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--151:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cache:disable`:anchor--152:

캐시 유형 비활성화

+---------------------------------------------------------------------+
| Code (language bash)                                                |
+=====================================================================+
| ```                                                                 |
| bin/magento cache:disable [--bootstrap BOOTSTRAP] [--] [<types>...] |
| ```                                                                 |
+---------------------------------------------------------------------+

### `types`:anchor--153:

공백으로 구분된 캐시 유형 목록 또는 모든 캐시 유형에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--bootstrap`:anchor--154:

부트스트랩의 매개 변수 추가 또는 재정의

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--155:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--156:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--157:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--158:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--159:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--160:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--161:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cache:enable`:anchor--162:

캐시 유형 활성화

+--------------------------------------------------------------------+
| Code (language bash)                                               |
+====================================================================+
| ```                                                                |
| bin/magento cache:enable [--bootstrap BOOTSTRAP] [--] [<types>...] |
| ```                                                                |
+--------------------------------------------------------------------+

### `types`:anchor--163:

공백으로 구분된 캐시 유형 목록 또는 모든 캐시 유형에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--bootstrap`:anchor--164:

부트스트랩의 매개 변수 추가 또는 재정의

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--165:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--166:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--167:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--168:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--169:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--170:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--171:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cache:flush`:anchor--172:

캐시 유형에서 사용하는 캐시 저장소를 플러시합니다.

+-------------------------------------------------------------------+
| Code (language bash)                                              |
+===================================================================+
| ```                                                               |
| bin/magento cache:flush [--bootstrap BOOTSTRAP] [--] [<types>...] |
| ```                                                               |
+-------------------------------------------------------------------+

### `types`:anchor--173:

공백으로 구분된 캐시 유형 목록 또는 모든 캐시 유형에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--bootstrap`:anchor--174:

부트스트랩의 매개 변수 추가 또는 재정의

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--175:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--176:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--177:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--178:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--179:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--180:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--181:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cache:status`:anchor--182:

캐시 상태 확인

+--------------------------------------------------+
| Code (language bash)                             |
+==================================================+
| ```                                              |
| bin/magento cache:status [--bootstrap BOOTSTRAP] |
| ```                                              |
+--------------------------------------------------+

### `--bootstrap`:anchor--183:

부트스트랩의 매개 변수 추가 또는 재정의

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--184:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--185:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--186:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--187:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--188:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--189:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--190:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `catalog:images:resize`:anchor--191:

크기 조정된 제품 이미지 만들기

+------------------------------------------------------------------------+
| Code (language bash)                                                   |
+========================================================================+
| ```                                                                    |
| bin/magento catalog:images:resize [-a\|--async] [--skip_hidden_images] |
| ```                                                                    |
+------------------------------------------------------------------------+

### `--async`, `-a`:anchor--192:

비동기 모드에서 이미지 크기 조정

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--skip_hidden_images`:anchor--193:

제품 페이지에서 숨김으로 표시된 이미지 처리 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--194:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--195:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--196:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--197:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--198:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--199:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--200:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `catalog:product:attributes:cleanup`:anchor--201:

사용하지 않는 제품 속성을 제거합니다.

+------------------------------------------------+
| Code (language bash)                           |
+================================================+
| ```                                            |
| bin/magento catalog:product:attributes:cleanup |
| ```                                            |
+------------------------------------------------+

### `--help`, `-h`:anchor--202:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--203:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--204:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--205:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--206:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--207:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--208:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cms:wysiwyg:restrict`:anchor--209:

사용자 HTML 컨텐츠 유효성 검사를 적용할지 또는 대신 경고를 표시할지 여부를 설정합니다.

+---------------------------------------------+
| Code (language bash)                        |
+=============================================+
| ```                                         |
| bin/magento cms:wysiwyg:restrict <restrict> |
| ```                                         |
+---------------------------------------------+

### `restrict`:anchor--210:

y\n

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--help`, `-h`:anchor--211:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--212:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--213:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--214:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--215:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--216:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--217:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `config:sensitive:set`:anchor--218:

중요 구성 값 설정

+----------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                       |
+============================================================================================================================+
| ```                                                                                                                        |
| bin/magento config:sensitive:set [-i\|--interactive] [--scope [SCOPE]] [--scope-code [SCOPE-CODE]] [--] [<path> [<value>]] |
| ```                                                                                                                        |
+----------------------------------------------------------------------------------------------------------------------------+

### `path`:anchor--219:

group/section/field\_name과 같은 구성 경로

### `value`:anchor--220:

구성 값

### `--interactive`, `-i`:anchor--221:

모든 중요 변수를 설정하려면 대화형 모드를 활성화하십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--scope`:anchor--222:

구성 범위, 설정하지 않은 경우 "기본값" 사용

+------------------+
| List (ol)        |
+==================+
| - 기본값: `default` |
| - 값을 허용합니다.      |
+------------------+

### `--scope-code`:anchor--223:

구성을 위한 범위 코드, 기본적으로 빈 문자열

+-------------+
| List (ol)   |
+=============+
| - 기본값: "    |
| - 값을 허용합니다. |
+-------------+

### `--help`, `-h`:anchor--224:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--225:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--226:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--227:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--228:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--229:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--230:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `config:set`:anchor--231:

시스템 구성 변경

+----------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                   |
+========================================================================================================================================+
| ```                                                                                                                                    |
| bin/magento config:set [--scope SCOPE] [--scope-code SCOPE-CODE] [-e\|--lock-env] [-c\|--lock-config] [-l\|--lock] [--] <path> <value> |
| ```                                                                                                                                    |
+----------------------------------------------------------------------------------------------------------------------------------------+

### `path`:anchor--232:

format section/group/field\_name의 구성 경로

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `value`:anchor--233:

구성 값

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--scope`:anchor--234:

구성 범위(기본값, 웹 사이트 또는 스토어)

+------------------+
| List (ol)        |
+==================+
| - 기본값: `default` |
| - 값 필요           |
+------------------+

### `--scope-code`:anchor--235:

범위 코드(범위가 '기본값'이 아닌 경우에만 필요)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-env`, `-e`:anchor--236:

관리자에서 수정할 수 없는 잠금 값(app/etc/env.php에 저장됨)

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--lock-config`, `-c`:anchor--237:

값을 잠그고 다른 설치와 공유하여 관리자의 수정을 방지합니다(app/etc/config.php에 저장됨).

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--lock`, `-l`:anchor--238:

사용하지 않음, 대신 —lock-env 옵션을 사용하십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--239:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--240:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--241:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--242:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--243:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--244:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--245:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `config:show`:anchor--246:

지정된 경로에 대한 구성 값을 표시합니다. 경로를 지정하지 않으면 저장된 값이 모두 표시됩니다

+-------------------------------------------------------------------------------------+
| Code (language bash)                                                                |
+=====================================================================================+
| ```                                                                                 |
| bin/magento config:show [--scope [SCOPE]] [--scope-code [SCOPE-CODE]] [--] [<path>] |
| ```                                                                                 |
+-------------------------------------------------------------------------------------+

### `path`:anchor--247:

구성 경로(예: section\_id/group\_id/field\_id)

### `--scope`:anchor--248:

구성 범위, 지정하지 않으면 '기본' 범위가 사용됩니다.

+------------------+
| List (ol)        |
+==================+
| - 기본값: `default` |
| - 값을 허용합니다.      |
+------------------+

### `--scope-code`:anchor--249:

범위 코드(범위가 아닌 경우에만 필요) `default`)

+-------------+
| List (ol)   |
+=============+
| - 기본값: "    |
| - 값을 허용합니다. |
+-------------+

### `--help`, `-h`:anchor--250:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--251:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--252:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--253:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--254:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--255:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--256:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cron:install`:anchor--257:

현재 사용자에 대한 crontab 생성 및 설치

+-------------------------------------------------------------+
| Code (language bash)                                        |
+=============================================================+
| ```                                                         |
| bin/magento cron:install [-f\|--force] [-d\|--non-optional] |
| ```                                                         |
+-------------------------------------------------------------+

### `--force`, `-f`:anchor--258:

설치 작업 강제 실행

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--non-optional`, `-d`:anchor--259:

선택 사항이 아닌 (기본) 작업만 설치

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--260:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--261:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--262:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--263:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--264:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--265:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--266:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cron:remove`:anchor--267:

crontab에서 작업 제거

+-------------------------+
| Code (language bash)    |
+=========================+
| ```                     |
| bin/magento cron:remove |
| ```                     |
+-------------------------+

### `--help`, `-h`:anchor--268:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--269:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--270:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--271:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--272:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--273:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--274:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `cron:run`:anchor--275:

일정별 작업 실행

+--------------------------------------------------------------+
| Code (language bash)                                         |
+==============================================================+
| ```                                                          |
| bin/magento cron:run [--group GROUP] [--bootstrap BOOTSTRAP] |
| ```                                                          |
+--------------------------------------------------------------+

### `--group`:anchor--276:

지정된 그룹에서만 작업 실행

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--bootstrap`:anchor--277:

부트스트랩의 매개 변수 추가 또는 재정의

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--278:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--279:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--280:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--281:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--282:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--283:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--284:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `customer:hash:upgrade`:anchor--285:

고객의 해시를 최신 알고리즘에 따라 업그레이드

+-----------------------------------+
| Code (language bash)              |
+===================================+
| ```                               |
| bin/magento customer:hash:upgrade |
| ```                               |
+-----------------------------------+

### `--help`, `-h`:anchor--286:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--287:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--288:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--289:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--290:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--291:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--292:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `deploy:mode:set`:anchor--293:

응용 프로그램 모드를 설정합니다.

+------------------------------------------------------------------+
| Code (language bash)                                             |
+==================================================================+
| ```                                                              |
| bin/magento deploy:mode:set [-s\|--skip-compilation] [--] <mode> |
| ```                                                              |
+------------------------------------------------------------------+

### `mode`:anchor--294:

설정할 애플리케이션 모드입니다. 사용 가능한 옵션은 "developer" 또는 "production"입니다.

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--skip-compilation`, `-s`:anchor--295:

정적 콘텐츠(생성된 코드, 사전 처리된 CSS 및 pub/static/의 자산)의 지우기 및 재생성을 건너뜁니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--296:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--297:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--298:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--299:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--300:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--301:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--302:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `deploy:mode:show`:anchor--303:

현재 응용 프로그램 모드를 표시합니다.

+------------------------------+
| Code (language bash)         |
+==============================+
| ```                          |
| bin/magento deploy:mode:show |
| ```                          |
+------------------------------+

### `--help`, `-h`:anchor--304:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--305:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--306:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--307:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--308:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--309:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--310:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:di:info`:anchor--311:

명령의 종속성 삽입 구성에 대한 정보를 제공합니다.

+---------------------------------+
| Code (language bash)            |
+=================================+
| ```                             |
| bin/magento dev:di:info <class> |
| ```                             |
+---------------------------------+

### `class`:anchor--312:

클래스 이름

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--help`, `-h`:anchor--313:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--314:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--315:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--316:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--317:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--318:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--319:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:email:newsletter-compatibility-check`:anchor--320:

뉴스레터 템플릿에서 잠재적인 변수 사용 호환성 문제를 검색합니다.

+------------------------------------------------------+
| Code (language bash)                                 |
+======================================================+
| ```                                                  |
| bin/magento dev:email:newsletter-compatibility-check |
| ```                                                  |
+------------------------------------------------------+

### `--help`, `-h`:anchor--321:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--322:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--323:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--324:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--325:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--326:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--327:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:email:override-compatibility-check`:anchor--328:

이메일 템플릿 재정의에서 잠재적인 변수 사용 호환성 문제를 검색합니다.

+----------------------------------------------------+
| Code (language bash)                               |
+====================================================+
| ```                                                |
| bin/magento dev:email:override-compatibility-check |
| ```                                                |
+----------------------------------------------------+

### `--help`, `-h`:anchor--329:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--330:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--331:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--332:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--333:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--334:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--335:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:profiler:disable`:anchor--336:

프로파일러를 비활성화합니다.

+----------------------------------+
| Code (language bash)             |
+==================================+
| ```                              |
| bin/magento dev:profiler:disable |
| ```                              |
+----------------------------------+

### `--help`, `-h`:anchor--337:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--338:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--339:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--340:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--341:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--342:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--343:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:profiler:enable`:anchor--344:

프로파일러를 활성화합니다.

+------------------------------------------+
| Code (language bash)                     |
+==========================================+
| ```                                      |
| bin/magento dev:profiler:enable [<type>] |
| ```                                      |
+------------------------------------------+

### `type`:anchor--345:

프로파일러 유형

### `--help`, `-h`:anchor--346:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--347:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--348:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--349:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--350:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--351:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--352:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:query-log:disable`:anchor--353:

DB 쿼리 로깅 비활성화

+-----------------------------------+
| Code (language bash)              |
+===================================+
| ```                               |
| bin/magento dev:query-log:disable |
| ```                               |
+-----------------------------------+

### `--help`, `-h`:anchor--354:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--355:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--356:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--357:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--358:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--359:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--360:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:query-log:enable`:anchor--361:

DB 쿼리 로깅 활성화

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento dev:query-log:enable [--include-all-queries [INCLUDE-ALL-QUERIES]] [--query-time-threshold [QUERY-TIME-THRESHOLD]] [--include-call-stack [I |
| NCLUDE-CALL-STACK]]                                                                                                                                      |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--include-all-queries`:anchor--362:

모든 쿼리를 기록합니다. \[true|false]

+---------------+
| List (ol)     |
+===============+
| - 기본값: `true` |
| - 값을 허용합니다.   |
+---------------+

### `--query-time-threshold`:anchor--363:

시간 임계값을 쿼리합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `0.001` |
| - 값을 허용합니다.    |
+----------------+

### `--include-call-stack`:anchor--364:

호출 스택을 포함합니다. \[true|false]

+---------------+
| List (ol)     |
+===============+
| - 기본값: `true` |
| - 값을 허용합니다.   |
+---------------+

### `--help`, `-h`:anchor--365:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--366:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--367:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--368:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--369:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--370:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--371:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:source-theme:deploy`:anchor--372:

테마의 소스 파일을 수집하고 게시합니다.

+--------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                               |
+====================================================================================================================+
| ```                                                                                                                |
| bin/magento dev:source-theme:deploy [--type TYPE] [--locale LOCALE] [--area AREA] [--theme THEME] [--] [<file>...] |
| ```                                                                                                                |
+--------------------------------------------------------------------------------------------------------------------+

### `file`:anchor--373:

사전 처리할 파일(확장명 없이 파일을 지정해야 함)

+-----------------------------------+
| List (ol)                         |
+===================================+
| - 기본값: `css/styles-mcss/styles-l` |
|                                   |
| - 배열                              |
+-----------------------------------+

### `--type`:anchor--374:

소스 파일 유형: \[간단히]

+---------------+
| List (ol)     |
+===============+
| - 기본값: `less` |
| - 값 필요        |
+---------------+

### `--locale`:anchor--375:

로케일: \[en\_US]

+----------------+
| List (ol)      |
+================+
| - 기본값: `en_US` |
| - 값 필요         |
+----------------+

### `--area`:anchor--376:

영역: \[frontend|adminhtml]

+-------------------+
| List (ol)         |
+===================+
| - 기본값: `frontend` |
| - 값 필요            |
+-------------------+

### `--theme`:anchor--377:

테마: \[공급업체/테마]

+-----------------------+
| List (ol)             |
+=======================+
| - 기본값: `Magento/luma` |
| - 값 필요                |
+-----------------------+

### `--help`, `-h`:anchor--378:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--379:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--380:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--381:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--382:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--383:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--384:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:template-hints:disable`:anchor--385:

프론트엔드 템플릿 힌트를 비활성화합니다. 캐시 플러시가 필요할 수 있습니다.

+----------------------------------------+
| Code (language bash)                   |
+========================================+
| ```                                    |
| bin/magento dev:template-hints:disable |
| ```                                    |
+----------------------------------------+

### `--help`, `-h`:anchor--386:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--387:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--388:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--389:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--390:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--391:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--392:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:template-hints:enable`:anchor--393:

프론트엔드 템플릿 힌트를 활성화합니다. 캐시 플러시가 필요할 수 있습니다.

+---------------------------------------+
| Code (language bash)                  |
+=======================================+
| ```                                   |
| bin/magento dev:template-hints:enable |
| ```                                   |
+---------------------------------------+

### `--help`, `-h`:anchor--394:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--395:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--396:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--397:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--398:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--399:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--400:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:template-hints:status`:anchor--401:

프론트엔드 템플릿 힌트 상태를 표시합니다.

+---------------------------------------+
| Code (language bash)                  |
+=======================================+
| ```                                   |
| bin/magento dev:template-hints:status |
| ```                                   |
+---------------------------------------+

### `--help`, `-h`:anchor--402:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--403:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--404:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--405:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--406:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--407:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--408:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:tests:run`:anchor--409:

테스트 실행

+---------------------------------------------------------------------+
| Code (language bash)                                                |
+=====================================================================+
| ```                                                                 |
| bin/magento dev:tests:run [-c\|--arguments ARGUMENTS] [--] [<type>] |
| ```                                                                 |
+---------------------------------------------------------------------+

### `type`:anchor--410:

실행할 테스트의 유형입니다. 사용 가능한 유형: all, unit, integration, integration-all, static, static-all, integrity, legacy, default

+------------------+
| List (ol)        |
+==================+
| - 기본값: `default` |
+------------------+

### `--arguments`, `-c`:anchor--411:

PHPUnit에 대한 추가 인수. 예: "-c'—filter=MyTest'" (공백 없음)

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--412:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--413:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--414:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--415:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--416:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--417:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--418:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:urn-catalog:generate`:anchor--419:

IDE에서 xml을 강조 표시하는 \*.xsd 매핑에 대한 URN 카탈로그를 생성합니다.

+--------------------------------------------------------------+
| Code (language bash)                                         |
+==============================================================+
| ```                                                          |
| bin/magento dev:urn-catalog:generate [--ide IDE] [--] <path> |
| ```                                                          |
+--------------------------------------------------------------+

### `path`:anchor--420:

카탈로그를 출력할 파일의 경로입니다. PhpStorm의 경우 .idea/misc.xml 를 사용합니다.

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--ide`:anchor--421:

카탈로그가 생성되는 형식입니다. 지원됨: \[phpstorm, vscode]

+-------------------+
| List (ol)         |
+===================+
| - 기본값: `phpstorm` |
| - 값 필요            |
+-------------------+

### `--help`, `-h`:anchor--422:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--423:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--424:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--425:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--426:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--427:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--428:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `dev:xml:convert`:anchor--429:

XSL 스타일 시트를 사용하여 XML 파일 변환

+---------------------------------------------------------------------------+
| Code (language bash)                                                      |
+===========================================================================+
| ```                                                                       |
| bin/magento dev:xml:convert [-o\|--overwrite] [--] <xml-file> <processor> |
| ```                                                                       |
+---------------------------------------------------------------------------+

### `xml-file`:anchor--430:

변환할 XML 파일의 경로

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `processor`:anchor--431:

XML 파일에 적용할 XSL 스타일 시트의 경로

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--overwrite`, `-o`:anchor--432:

XML 파일 덮어쓰기

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--433:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--434:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--435:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--436:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--437:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--438:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--439:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `downloadable:domains:add`:anchor--440:

다운로드 가능한 도메인 허용 목록에 도메인 추가

+-----------------------------------------------------+
| Code (language bash)                                |
+=====================================================+
| ```                                                 |
| bin/magento downloadable:domains:add [<domains>...] |
| ```                                                 |
+-----------------------------------------------------+

### `domains`:anchor--441:

도메인 이름

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--442:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--443:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--444:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--445:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--446:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--447:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--448:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `downloadable:domains:remove`:anchor--449:

다운로드 가능한 도메인 허용 목록에서 도메인 제거

+--------------------------------------------------------+
| Code (language bash)                                   |
+========================================================+
| ```                                                    |
| bin/magento downloadable:domains:remove [<domains>...] |
| ```                                                    |
+--------------------------------------------------------+

### `domains`:anchor--450:

도메인 이름

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--451:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--452:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--453:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--454:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--455:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--456:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--457:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `downloadable:domains:show`:anchor--458:

다운로드 가능한 도메인을 허용 목록에 표시

+---------------------------------------+
| Code (language bash)                  |
+=======================================+
| ```                                   |
| bin/magento downloadable:domains:show |
| ```                                   |
+---------------------------------------+

### `--help`, `-h`:anchor--459:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--460:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--461:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--462:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--463:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--464:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--465:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `encryption:payment-data:update`:anchor--466:

암호화된 신용 카드 데이터를 최신 암호화 암호로 다시 암호화합니다.

+--------------------------------------------+
| Code (language bash)                       |
+============================================+
| ```                                        |
| bin/magento encryption:payment-data:update |
| ```                                        |
+--------------------------------------------+

### `--help`, `-h`:anchor--467:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--468:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--469:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--470:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--471:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--472:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--473:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `i18n:collect-phrases`:anchor--474:

코드베이스에서 구 검색

+-------------------------------------------------------------------------------------------+
| Code (language bash)                                                                      |
+===========================================================================================+
| ```                                                                                       |
| bin/magento i18n:collect-phrases [-o\|--output OUTPUT] [-m\|--magento] [--] [<directory>] |
| ```                                                                                       |
+-------------------------------------------------------------------------------------------+

### `directory`:anchor--475:

구문 분석할 디렉터리 경로. —magento 플래그가 설정된 경우 필요 없음

### `--output`, `-o`:anchor--476:

출력 파일 경로(파일 이름 포함). 지정된 파일이 없으면 기본값은 stdout입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--magento`, `-m`:anchor--477:

—magento 매개 변수를 사용하여 현재 Magento 코드베이스를 구문 분석합니다. 디렉토리가 지정된 경우 매개변수를 생략합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--478:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--479:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--480:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--481:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--482:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--483:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--484:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `i18n:pack`:anchor--485:

언어 패키지를 저장합니다.

+-----------------------------------------------------------------------------------------+
| Code (language bash)                                                                    |
+=========================================================================================+
| ```                                                                                     |
| bin/magento i18n:pack [-m\|--mode MODE] [-d\|--allow-duplicates] [--] <source> <locale> |
| ```                                                                                     |
+-----------------------------------------------------------------------------------------+

### `source`:anchor--486:

번역이 있는 소스 사전 파일의 경로

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `locale`:anchor--487:

사전의 대상 로케일(예: "de\_DE")

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--mode`, `-m`:anchor--488:

사전의 저장 모드 - "바꾸기" - 새 언어 팩으로 바꾸기 - "병합" - 언어 패키지 병합, 기본적으로 "바꾸기"

+------------------+
| List (ol)        |
+==================+
| - 기본값: `replace` |
| - 값 필요           |
+------------------+

### `--allow-duplicates`, `-d`:anchor--489:

—allow-duplicates 매개 변수를 사용하여 번역의 중복을 저장할 수 있습니다. 그렇지 않으면 매개 변수를 생략합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--490:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--491:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--492:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--493:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--494:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--495:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--496:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `i18n:uninstall`:anchor--497:

언어 패키지를 제거합니다.

+------------------------------------------------------------------+
| Code (language bash)                                             |
+==================================================================+
| ```                                                              |
| bin/magento i18n:uninstall [-b\|--backup-code] [--] <package>... |
| ```                                                              |
+------------------------------------------------------------------+

### `package`:anchor--498:

언어 패키지 이름

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 필수        |
|             |
| - 배열        |
+-------------+

### `--backup-code`, `-b`:anchor--499:

코드 및 구성 파일 백업 수행(임시 파일 제외)

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--500:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--501:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--502:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--503:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--504:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--505:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--506:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `indexer:info`:anchor--507:

허용된 인덱서를 표시합니다.

+--------------------------+
| Code (language bash)     |
+==========================+
| ```                      |
| bin/magento indexer:info |
| ```                      |
+--------------------------+

### `--help`, `-h`:anchor--508:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--509:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--510:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--511:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--512:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--513:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--514:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `indexer:reindex`:anchor--515:

데이터 다시 인덱싱

+------------------------------------------+
| Code (language bash)                     |
+==========================================+
| ```                                      |
| bin/magento indexer:reindex [<index>...] |
| ```                                      |
+------------------------------------------+

### `index`:anchor--516:

공백으로 구분된 색인 유형 목록 또는 모든 색인에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--517:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--518:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--519:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--520:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--521:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--522:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--523:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `indexer:reset`:anchor--524:

인덱서 상태를 잘못된 상태로 다시 설정합니다.

+----------------------------------------+
| Code (language bash)                   |
+========================================+
| ```                                    |
| bin/magento indexer:reset [<index>...] |
| ```                                    |
+----------------------------------------+

### `index`:anchor--525:

공백으로 구분된 색인 유형 목록 또는 모든 색인에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--526:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--527:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--528:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--529:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--530:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--531:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--532:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `indexer:set-dimensions-mode`:anchor--533:

인덱서 Dimension 모드 설정

+--------------------------------------------------------------+
| Code (language bash)                                         |
+==============================================================+
| ```                                                          |
| bin/magento indexer:set-dimensions-mode [<indexer> [<mode>]] |
| ```                                                          |
+--------------------------------------------------------------+

### `indexer`:anchor--534:

인덱서 이름 \[catalog\_product\_price]

### `mode`:anchor--535:

인덱서 차원 모드 catalog\_product\_price none,website,customer\_group,website\_and\_customer\_group

### `--help`, `-h`:anchor--536:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--537:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--538:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--539:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--540:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--541:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--542:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `indexer:set-mode`:anchor--543:

인덱스 모드 유형 설정

+----------------------------------------------------+
| Code (language bash)                               |
+====================================================+
| ```                                                |
| bin/magento indexer:set-mode [<mode> [<index>...]] |
| ```                                                |
+----------------------------------------------------+

### `mode`:anchor--544:

인덱서 모드 유형 \[실시간|예약]

### `index`:anchor--545:

공백으로 구분된 색인 유형 목록 또는 모든 색인에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--546:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--547:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--548:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--549:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--550:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--551:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--552:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `indexer:show-dimensions-mode`:anchor--553:

인덱서 Dimension 모드 표시

+---------------------------------------------------------+
| Code (language bash)                                    |
+=========================================================+
| ```                                                     |
| bin/magento indexer:show-dimensions-mode [<indexer>...] |
| ```                                                     |
+---------------------------------------------------------+

### `indexer`:anchor--554:

공간으로 구분된 색인 유형 목록 또는 모든 색인에 적용할 생략(catalog\_product\_price)

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--555:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--556:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--557:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--558:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--559:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--560:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--561:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `indexer:show-mode`:anchor--562:

색인 모드 표시

+--------------------------------------------+
| Code (language bash)                       |
+============================================+
| ```                                        |
| bin/magento indexer:show-mode [<index>...] |
| ```                                        |
+--------------------------------------------+

### `index`:anchor--563:

공백으로 구분된 색인 유형 목록 또는 모든 색인에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--564:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--565:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--566:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--567:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--568:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--569:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--570:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `indexer:status`:anchor--571:

인덱서의 상태를 표시합니다.

+-----------------------------------------+
| Code (language bash)                    |
+=========================================+
| ```                                     |
| bin/magento indexer:status [<index>...] |
| ```                                     |
+-----------------------------------------+

### `index`:anchor--572:

공백으로 구분된 색인 유형 목록 또는 모든 색인에 적용하기 위한 생략

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--573:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--574:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--575:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--576:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--577:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--578:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--579:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `info:adminuri`:anchor--580:

Magento 관리자 URI를 표시합니다

+---------------------------+
| Code (language bash)      |
+===========================+
| ```                       |
| bin/magento info:adminuri |
| ```                       |
+---------------------------+

### `--help`, `-h`:anchor--581:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--582:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--583:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--584:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--585:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--586:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--587:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `info:backups:list`:anchor--588:

사용 가능한 백업 파일 목록을 인쇄합니다.

+-------------------------------+
| Code (language bash)          |
+===============================+
| ```                           |
| bin/magento info:backups:list |
| ```                           |
+-------------------------------+

### `--help`, `-h`:anchor--589:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--590:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--591:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--592:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--593:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--594:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--595:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `info:currency:list`:anchor--596:

사용 가능한 통화 목록을 표시합니다.

+--------------------------------+
| Code (language bash)           |
+================================+
| ```                            |
| bin/magento info:currency:list |
| ```                            |
+--------------------------------+

### `--help`, `-h`:anchor--597:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--598:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--599:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--600:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--601:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--602:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--603:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `info:dependencies:show-framework`:anchor--604:

Magento 프레임워크의 종속성 수를 표시합니다.

+--------------------------------------------------------------------+
| Code (language bash)                                               |
+====================================================================+
| ```                                                                |
| bin/magento info:dependencies:show-framework [-o\|--output OUTPUT] |
| ```                                                                |
+--------------------------------------------------------------------+

### `--output`, `-o`:anchor--605:

보고서 파일 이름

+-------------------------------------+
| List (ol)                           |
+=====================================+
| - 기본값: `framework-dependencies.csv` |
| - 값 필요                              |
+-------------------------------------+

### `--help`, `-h`:anchor--606:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--607:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--608:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--609:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--610:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--611:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--612:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `info:dependencies:show-modules`:anchor--613:

모듈 간 종속성 수를 표시합니다.

+------------------------------------------------------------------+
| Code (language bash)                                             |
+==================================================================+
| ```                                                              |
| bin/magento info:dependencies:show-modules [-o\|--output OUTPUT] |
| ```                                                              |
+------------------------------------------------------------------+

### `--output`, `-o`:anchor--614:

보고서 파일 이름

+-----------------------------------+
| List (ol)                         |
+===================================+
| - 기본값: `modules-dependencies.csv` |
| - 값 필요                            |
+-----------------------------------+

### `--help`, `-h`:anchor--615:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--616:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--617:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--618:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--619:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--620:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--621:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `info:dependencies:show-modules-circular`:anchor--622:

모듈 간 순환 종속성 수를 표시합니다.

+---------------------------------------------------------------------------+
| Code (language bash)                                                      |
+===========================================================================+
| ```                                                                       |
| bin/magento info:dependencies:show-modules-circular [-o\|--output OUTPUT] |
| ```                                                                       |
+---------------------------------------------------------------------------+

### `--output`, `-o`:anchor--623:

보고서 파일 이름

+--------------------------------------------+
| List (ol)                                  |
+============================================+
| - 기본값: `modules-circular-dependencies.csv` |
| - 값 필요                                     |
+--------------------------------------------+

### `--help`, `-h`:anchor--624:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--625:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--626:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--627:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--628:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--629:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--630:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `info:language:list`:anchor--631:

사용 가능한 언어 로케일 목록을 표시합니다.

+--------------------------------+
| Code (language bash)           |
+================================+
| ```                            |
| bin/magento info:language:list |
| ```                            |
+--------------------------------+

### `--help`, `-h`:anchor--632:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--633:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--634:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--635:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--636:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--637:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--638:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `info:timezone:list`:anchor--639:

사용 가능한 시간대 목록을 표시합니다.

+--------------------------------+
| Code (language bash)           |
+================================+
| ```                            |
| bin/magento info:timezone:list |
| ```                            |
+--------------------------------+

### `--help`, `-h`:anchor--640:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--641:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--642:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--643:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--644:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--645:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--646:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `inventory:reservation:create-compensations`:anchor--647:

제공된 보상 인수로 예약 만들기

+----------------------------------------------------------------------------------------------+
| Code (language bash)                                                                         |
+==============================================================================================+
| ```                                                                                          |
| bin/magento inventory:reservation:create-compensations [-r\|--raw] [--] [<compensations>...] |
| ```                                                                                          |
+----------------------------------------------------------------------------------------------+

### `compensations`:anchor--648:

"\ 형식의 보상 인수 목록\<order\_increment\_id>:\&lt;sku>:\&lt;quantity>:\&lt;stock-id>"

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--raw`, `-r`:anchor--649:

원시 출력

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--650:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--651:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--652:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--653:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--654:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--655:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--656:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `inventory:reservation:list-inconsistencies`:anchor--657:

판매 가능한 수량이 일치하지 않는 모든 주문 및 제품 표시

+------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                 |
+======================================================================================================================================================+
| ```                                                                                                                                                  |
| bin/magento inventory:reservation:list-inconsistencies [-c\|--complete-orders] [-i\|--incomplete-orders] [-b\|--bunch-size [BUNCH-SIZE]] [-r\|--raw] |
| ```                                                                                                                                                  |
+------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--complete-orders`, `-c`:anchor--658:

전체 주문에 대한 불일치만 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--incomplete-orders`, `-i`:anchor--659:

미완료 주문에 대한 불일치만 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--bunch-size`, `-b`:anchor--660:

한 번에 로드할 주문 수를 정의합니다.

+-------------+
| List (ol)   |
+=============+
| - 기본값: `50` |
| - 값을 허용합니다. |
+-------------+

### `--raw`, `-r`:anchor--661:

원시 출력

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--662:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--663:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--664:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--665:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--666:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--667:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--668:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `inventory-geonames:import`:anchor--669:

소스 선택 알고리즘에 대한 지역 이름 다운로드 및 가져오기

+------------------------------------------------------+
| Code (language bash)                                 |
+======================================================+
| ```                                                  |
| bin/magento inventory-geonames:import <countries>... |
| ```                                                  |
+------------------------------------------------------+

### `countries`:anchor--670:

가져올 국가 코드 목록

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 필수        |
|             |
| - 배열        |
+-------------+

### `--help`, `-h`:anchor--671:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--672:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--673:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--674:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--675:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--676:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--677:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `maintenance:allow-ips`:anchor--678:

유지 관리 모드 제외 IP 설정

+---------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                          |
+===============================================================================================================+
| ```                                                                                                           |
| bin/magento maintenance:allow-ips [--none] [--add] [--magento-init-params MAGENTO-INIT-PARAMS] [--] [<ip>...] |
| ```                                                                                                           |
+---------------------------------------------------------------------------------------------------------------+

### `ip`:anchor--679:

허용된 IP 주소

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--none`:anchor--680:

허용된 IP 주소 지우기

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--add`:anchor--681:

기존 목록에 IP 주소 추가

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--magento-init-params`:anchor--682:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--683:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--684:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--685:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--686:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--687:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--688:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--689:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `maintenance:disable`:anchor--690:

유지 관리 모드 비활성화

+---------------------------------------------------------------------------------------+
| Code (language bash)                                                                  |
+=======================================================================================+
| ```                                                                                   |
| bin/magento maintenance:disable [--ip IP] [--magento-init-params MAGENTO-INIT-PARAMS] |
| ```                                                                                   |
+---------------------------------------------------------------------------------------+

### `--ip`:anchor--691:

허용된 IP 주소(허용된 IP 목록을 지우려면 '없음'을 사용)

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
| - 값 필요      |
+-------------+

### `--magento-init-params`:anchor--692:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--693:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--694:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--695:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--696:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--697:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--698:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--699:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `maintenance:enable`:anchor--700:

유지 관리 모드 활성화

+--------------------------------------------------------------------------------------+
| Code (language bash)                                                                 |
+======================================================================================+
| ```                                                                                  |
| bin/magento maintenance:enable [--ip IP] [--magento-init-params MAGENTO-INIT-PARAMS] |
| ```                                                                                  |
+--------------------------------------------------------------------------------------+

### `--ip`:anchor--701:

허용된 IP 주소(허용된 IP 목록을 지우려면 '없음'을 사용)

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
| - 값 필요      |
+-------------+

### `--magento-init-params`:anchor--702:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--703:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--704:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--705:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--706:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--707:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--708:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--709:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `maintenance:status`:anchor--710:

유지 관리 모드 상태 표시

+----------------------------------------------------------------------------+
| Code (language bash)                                                       |
+============================================================================+
| ```                                                                        |
| bin/magento maintenance:status [--magento-init-params MAGENTO-INIT-PARAMS] |
| ```                                                                        |
+----------------------------------------------------------------------------+

### `--magento-init-params`:anchor--711:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--712:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--713:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--714:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--715:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--716:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--717:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--718:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `media-content:sync`:anchor--719:

컨텐츠를 자산과 동기화

+--------------------------------+
| Code (language bash)           |
+================================+
| ```                            |
| bin/magento media-content:sync |
| ```                            |
+--------------------------------+

### `--help`, `-h`:anchor--720:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--721:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--722:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--723:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--724:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--725:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--726:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `media-gallery:sync`:anchor--727:

데이터베이스의 미디어 저장소 및 미디어 자산 동기화

+--------------------------------+
| Code (language bash)           |
+================================+
| ```                            |
| bin/magento media-gallery:sync |
| ```                            |
+--------------------------------+

### `--help`, `-h`:anchor--728:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--729:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--730:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--731:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--732:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--733:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--734:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `module:config:status`:anchor--735:

'app/etc/config.php' 파일에서 모듈 구성을 확인하고 최신 상태인지 보고합니다.

+----------------------------------+
| Code (language bash)             |
+==================================+
| ```                              |
| bin/magento module:config:status |
| ```                              |
+----------------------------------+

### `--help`, `-h`:anchor--736:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--737:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--738:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--739:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--740:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--741:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--742:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `module:disable`:anchor--743:

지정된 모듈 비활성화

+----------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                         |
+==============================================================================================================================================+
| ```                                                                                                                                          |
| bin/magento module:disable [-f\|--force] [--all] [-c\|--clear-static-content] [--magento-init-params MAGENTO-INIT-PARAMS] [--] [<module>...] |
| ```                                                                                                                                          |
+----------------------------------------------------------------------------------------------------------------------------------------------+

### `module`:anchor--744:

모듈 이름

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--force`, `-f`:anchor--745:

종속성 확인 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--all`:anchor--746:

모든 모듈 비활성화

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--clear-static-content`, `-c`:anchor--747:

생성된 정적 보기 파일을 지웁니다. 모듈에 정적 보기 파일이 있는 경우 필요합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--magento-init-params`:anchor--748:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--749:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--750:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--751:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--752:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--753:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--754:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--755:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `module:enable`:anchor--756:

지정된 모듈 활성화

+---------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                        |
+=============================================================================================================================================+
| ```                                                                                                                                         |
| bin/magento module:enable [-f\|--force] [--all] [-c\|--clear-static-content] [--magento-init-params MAGENTO-INIT-PARAMS] [--] [<module>...] |
| ```                                                                                                                                         |
+---------------------------------------------------------------------------------------------------------------------------------------------+

### `module`:anchor--757:

모듈 이름

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--force`, `-f`:anchor--758:

종속성 확인 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--all`:anchor--759:

모든 모듈 활성화

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--clear-static-content`, `-c`:anchor--760:

생성된 정적 보기 파일을 지웁니다. 모듈에 정적 보기 파일이 있는 경우 필요합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--magento-init-params`:anchor--761:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--762:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--763:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--764:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--765:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--766:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--767:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--768:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `module:status`:anchor--769:

모듈 상태 표시

+-------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                    |
+=========================================================================================================================+
| ```                                                                                                                     |
| bin/magento module:status [--enabled] [--disabled] [--magento-init-params MAGENTO-INIT-PARAMS] [--] [<module-names>...] |
| ```                                                                                                                     |
+-------------------------------------------------------------------------------------------------------------------------+

### `module-names`:anchor--770:

선택적 모듈 이름

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--enabled`:anchor--771:

활성화된 모듈만 인쇄

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--disabled`:anchor--772:

비활성화된 모듈만 인쇄

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--magento-init-params`:anchor--773:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--774:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--775:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--776:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--777:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--778:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--779:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--780:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `module:uninstall`:anchor--781:

Composer에서 설치한 모듈 제거

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento module:uninstall [-r\|--remove-data] [--backup-code] [--backup-media] [--backup-db] [--non-composer] [-c\|--clear-static-content] [--magent |
| o-init-params MAGENTO-INIT-PARAMS] [--] <module>...                                                                                                      |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `module`:anchor--782:

모듈 이름

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 필수        |
|             |
| - 배열        |
+-------------+

### `--remove-data`, `-r`:anchor--783:

모듈이 설치한 데이터 제거

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--backup-code`:anchor--784:

코드 및 구성 파일 백업 수행(임시 파일 제외)

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--backup-media`:anchor--785:

미디어 백업 수행

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--backup-db`:anchor--786:

전체 데이터베이스 백업 수행

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--non-composer`:anchor--787:

여기에서 지나간 모든 모듈은 작성기를 기반으로 하지 않습니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--clear-static-content`, `-c`:anchor--788:

생성된 정적 보기 파일을 지웁니다. 모듈에 정적 보기 파일이 있는 경우 필요합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--magento-init-params`:anchor--789:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--790:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--791:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--792:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--793:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--794:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--795:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--796:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `newrelic:create:deploy-marker`:anchor--797:

배포 큐에서 항목을 확인하고 적절한 배포 마커를 만듭니다.

+----------------------------------------------------------------------------------------+
| Code (language bash)                                                                   |
+========================================================================================+
| ```                                                                                    |
| bin/magento newrelic:create:deploy-marker <message> <change_log> [<user> [<revision>]] |
| ```                                                                                    |
+----------------------------------------------------------------------------------------+

### `message`:anchor--798:

메시지를 배포하시겠습니까?

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `change_log`:anchor--799:

변경 로그?

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `user`:anchor--800:

배포 사용자

### `revision`:anchor--801:

개정

### `--help`, `-h`:anchor--802:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--803:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--804:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--805:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--806:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--807:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--808:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `queue:consumers:list`:anchor--809:

MessageQueue 소비자 목록

+----------------------------------+
| Code (language bash)             |
+==================================+
| ```                              |
| bin/magento queue:consumers:list |
| ```                              |
+----------------------------------+

### `--help`, `-h`:anchor--810:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--811:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--812:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--813:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--814:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--815:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--816:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `queue:consumers:restart`:anchor--817:

MessageQueue 소비자 다시 시작

+-------------------------------------+
| Code (language bash)                |
+=====================================+
| ```                                 |
| bin/magento queue:consumers:restart |
| ```                                 |
+-------------------------------------+

### `--help`, `-h`:anchor--818:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--819:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--820:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--821:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--822:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--823:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--824:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `queue:consumers:start`:anchor--825:

MessageQueue 소비자 시작

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento queue:consumers:start [--max-messages MAX-MESSAGES] [--batch-size BATCH-SIZE] [--area-code AREA-CODE] [--single-thread] [--multi-process [M |
| ULTI-PROCESS]] [--pid-file-path PID-FILE-PATH] [--] <consumer>                                                                                           |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `consumer`:anchor--826:

시작할 소비자의 이름입니다.

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--max-messages`:anchor--827:

프로세스 종료 전 소비자가 처리한 메시지 수입니다. 지정하지 않은 경우 - 큐에 있는 모든 메시지를 처리한 후 종료합니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--batch-size`:anchor--828:

일괄 처리당 메시지 수. 배치 소비자에 대해서만 적용할 수 있습니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--area-code`:anchor--829:

기본 설정 영역(전역, adminhtml 등) 기본값은 전역입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--single-thread`:anchor--830:

이 옵션은 한 소비자의 여러 복사본을 동시에 실행하지 않도록 합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--multi-process`:anchor--831:

소비자당 프로세스 수.

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--pid-file-path`:anchor--832:

PID 저장을 위한 파일 경로(이 옵션은 더 이상 사용되지 않으며 대신 —단일 스레드 사용)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--833:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--834:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--835:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--836:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--837:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--838:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--839:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `remote-storage:sync`:anchor--840:

미디어 파일을 원격 스토리지와 동기화합니다.

+---------------------------------+
| Code (language bash)            |
+=================================+
| ```                             |
| bin/magento remote-storage:sync |
| ```                             |
+---------------------------------+

### `--help`, `-h`:anchor--841:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--842:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--843:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--844:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--845:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--846:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--847:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `sampledata:deploy`:anchor--848:

작성기 기반 Magento 설치를 위한 샘플 데이터 모듈 배포

+---------------------------------------------+
| Code (language bash)                        |
+=============================================+
| ```                                         |
| bin/magento sampledata:deploy [--no-update] |
| ```                                         |
+---------------------------------------------+

### `--no-update`:anchor--849:

작성기 업데이트를 실행하지 않고 composer.json 업데이트

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--850:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--851:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--852:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--853:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--854:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--855:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--856:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `sampledata:remove`:anchor--857:

composer.json에서 모든 샘플 데이터 패키지 제거

+---------------------------------------------+
| Code (language bash)                        |
+=============================================+
| ```                                         |
| bin/magento sampledata:remove [--no-update] |
| ```                                         |
+---------------------------------------------+

### `--no-update`:anchor--858:

작성기 업데이트를 실행하지 않고 composer.json 업데이트

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--859:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--860:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--861:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--862:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--863:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--864:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--865:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `sampledata:reset`:anchor--866:

재설치를 위해 모든 샘플 데이터 모듈 재설정

+------------------------------+
| Code (language bash)         |
+==============================+
| ```                          |
| bin/magento sampledata:reset |
| ```                          |
+------------------------------+

### `--help`, `-h`:anchor--867:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--868:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--869:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--870:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--871:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--872:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--873:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `security:recaptcha:disable-for-user-forgot-password`:anchor--874:

관리자가 암호 찾기 양식에 대해 reCAPTCHA 비활성화

+-----------------------------------------------------------------+
| Code (language bash)                                            |
+=================================================================+
| ```                                                             |
| bin/magento security:recaptcha:disable-for-user-forgot-password |
| ```                                                             |
+-----------------------------------------------------------------+

### `--help`, `-h`:anchor--875:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--876:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--877:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--878:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--879:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--880:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--881:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `security:recaptcha:disable-for-user-login`:anchor--882:

관리자 로그인 양식에 대해 reCAPTCHA 비활성화

+-------------------------------------------------------+
| Code (language bash)                                  |
+=======================================================+
| ```                                                   |
| bin/magento security:recaptcha:disable-for-user-login |
| ```                                                   |
+-------------------------------------------------------+

### `--help`, `-h`:anchor--883:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--884:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--885:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--886:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--887:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--888:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--889:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `security:tfa:google:set-secret`:anchor--890:

Google OTP 생성에 사용되는 암호를 설정합니다.

+------------------------------------------------------------+
| Code (language bash)                                       |
+============================================================+
| ```                                                        |
| bin/magento security:tfa:google:set-secret <user> <secret> |
| ```                                                        |
+------------------------------------------------------------+

### `user`:anchor--891:

사용자 이름

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `secret`:anchor--892:

암호

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--help`, `-h`:anchor--893:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--894:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--895:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--896:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--897:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--898:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--899:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `security:tfa:providers`:anchor--900:

사용 가능한 모든 공급자 나열

+------------------------------------+
| Code (language bash)               |
+====================================+
| ```                                |
| bin/magento security:tfa:providers |
| ```                                |
+------------------------------------+

### `--help`, `-h`:anchor--901:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--902:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--903:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--904:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--905:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--906:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--907:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `security:tfa:reset`:anchor--908:

한 명의 사용자에 대한 구성 재설정

+--------------------------------------------------+
| Code (language bash)                             |
+==================================================+
| ```                                              |
| bin/magento security:tfa:reset <user> <provider> |
| ```                                              |
+--------------------------------------------------+

### `user`:anchor--909:

사용자 이름

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `provider`:anchor--910:

공급자 코드

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--help`, `-h`:anchor--911:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--912:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--913:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--914:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--915:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--916:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--917:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:backup`:anchor--918:

Magento 애플리케이션 코드 베이스, 미디어 및 데이터베이스의 백업 수행

+------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                           |
+================================================================================================+
| ```                                                                                            |
| bin/magento setup:backup [--code] [--media] [--db] [--magento-init-params MAGENTO-INIT-PARAMS] |
| ```                                                                                            |
+------------------------------------------------------------------------------------------------+

### `--code`:anchor--919:

코드 및 구성 파일 백업 수행(임시 파일 제외)

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--media`:anchor--920:

미디어 백업 수행

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--db`:anchor--921:

전체 데이터베이스 백업 수행

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--magento-init-params`:anchor--922:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--923:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--924:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--925:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--926:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--927:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--928:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--929:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:config:set`:anchor--930:

배포 구성을 만들거나 수정합니다.

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento setup:config:set [--enable-debug-logging ENABLE-DEBUG-LOGGING] [--enable-syslog-logging ENABLE-SYSLOG-LOGGING] [--backend-frontname BACKEND |
| -FRONTNAME] [--id_salt ID_SALT] [--remote-storage-driver REMOTE-STORAGE-DRIVER] [--remote-storage-prefix REMOTE-STORAGE-PREFIX] [--remote-storage-endpo |
| int REMOTE-STORAGE-ENDPOINT] [--remote-storage-bucket REMOTE-STORAGE-BUCKET] [--remote-storage-region REMOTE-STORAGE-REGION] [--remote-storage-key REMO |
| TE-STORAGE-KEY] [--remote-storage-secret REMOTE-STORAGE-SECRET] [--remote-storage-path-style REMOTE-STORAGE-PATH-STYLE] [--amqp-host AMQP-HOST] [--amqp |
| -port AMQP-PORT] [--amqp-user AMQP-USER] [--amqp-password AMQP-PASSWORD] [--amqp-virtualhost AMQP-VIRTUALHOST] [--amqp-ssl AMQP-SSL] [--amqp-ssl-option |
| s AMQP-SSL-OPTIONS] [--consumers-wait-for-messages CONSUMERS-WAIT-FOR-MESSAGES] [--queue-default-connection QUEUE-DEFAULT-CONNECTION] [--key KEY] [--db |
| -host DB-HOST] [--db-name DB-NAME] [--db-user DB-USER] [--db-engine DB-ENGINE] [--db-password DB-PASSWORD] [--db-prefix DB-PREFIX] [--db-model DB-MODEL |
| ] [--db-init-statements DB-INIT-STATEMENTS] [-s\|--skip-db-validation] [--http-cache-hosts HTTP-CACHE-HOSTS] [--db-ssl-key DB-SSL-KEY] [--db-ssl-cert D |
| B-SSL-CERT] [--db-ssl-ca DB-SSL-CA] [--db-ssl-verify] [--session-save SESSION-SAVE] [--session-save-redis-host SESSION-SAVE-REDIS-HOST] [--session-save |
| -redis-port SESSION-SAVE-REDIS-PORT] [--session-save-redis-password SESSION-SAVE-REDIS-PASSWORD] [--session-save-redis-timeout SESSION-SAVE-REDIS-TIMEO |
| UT] [--session-save-redis-persistent-id SESSION-SAVE-REDIS-PERSISTENT-ID] [--session-save-redis-db SESSION-SAVE-REDIS-DB] [--session-save-redis-compres |
| sion-threshold SESSION-SAVE-REDIS-COMPRESSION-THRESHOLD] [--session-save-redis-compression-lib SESSION-SAVE-REDIS-COMPRESSION-LIB] [--session-save-redi |
| s-log-level SESSION-SAVE-REDIS-LOG-LEVEL] [--session-save-redis-max-concurrency SESSION-SAVE-REDIS-MAX-CONCURRENCY] [--session-save-redis-break-after-f |
| rontend SESSION-SAVE-REDIS-BREAK-AFTER-FRONTEND] [--session-save-redis-break-after-adminhtml SESSION-SAVE-REDIS-BREAK-AFTER-ADMINHTML] [--session-save- |
| redis-first-lifetime SESSION-SAVE-REDIS-FIRST-LIFETIME] [--session-save-redis-bot-first-lifetime SESSION-SAVE-REDIS-BOT-FIRST-LIFETIME] [--session-save |
| -redis-bot-lifetime SESSION-SAVE-REDIS-BOT-LIFETIME] [--session-save-redis-disable-locking SESSION-SAVE-REDIS-DISABLE-LOCKING] [--session-save-redis-mi |
| n-lifetime SESSION-SAVE-REDIS-MIN-LIFETIME] [--session-save-redis-max-lifetime SESSION-SAVE-REDIS-MAX-LIFETIME] [--session-save-redis-sentinel-master S |
| ESSION-SAVE-REDIS-SENTINEL-MASTER] [--session-save-redis-sentinel-servers SESSION-SAVE-REDIS-SENTINEL-SERVERS] [--session-save-redis-sentinel-verify-ma |
| ster SESSION-SAVE-REDIS-SENTINEL-VERIFY-MASTER] [--session-save-redis-sentinel-connect-retries SESSION-SAVE-REDIS-SENTINEL-CONNECT-RETRIES] [--cache-ba |
| ckend CACHE-BACKEND] [--cache-backend-redis-server CACHE-BACKEND-REDIS-SERVER] [--cache-backend-redis-db CACHE-BACKEND-REDIS-DB] [--cache-backend-redis |
| -port CACHE-BACKEND-REDIS-PORT] [--cache-backend-redis-password CACHE-BACKEND-REDIS-PASSWORD] [--cache-backend-redis-compress-data CACHE-BACKEND-REDIS- |
| COMPRESS-DATA] [--cache-backend-redis-compression-lib CACHE-BACKEND-REDIS-COMPRESSION-LIB] [--cache-id-prefix CACHE-ID-PREFIX] [--allow-parallel-genera |
| tion] [--page-cache PAGE-CACHE] [--page-cache-redis-server PAGE-CACHE-REDIS-SERVER] [--page-cache-redis-db PAGE-CACHE-REDIS-DB] [--page-cache-redis-por |
| t PAGE-CACHE-REDIS-PORT] [--page-cache-redis-password PAGE-CACHE-REDIS-PASSWORD] [--page-cache-redis-compress-data PAGE-CACHE-REDIS-COMPRESS-DATA] [--p |
| age-cache-redis-compression-lib PAGE-CACHE-REDIS-COMPRESSION-LIB] [--page-cache-id-prefix PAGE-CACHE-ID-PREFIX] [--lock-provider LOCK-PROVIDER] [--lock |
| -db-prefix LOCK-DB-PREFIX] [--lock-zookeeper-host LOCK-ZOOKEEPER-HOST] [--lock-zookeeper-path LOCK-ZOOKEEPER-PATH] [--lock-file-path LOCK-FILE-PATH] [- |
| -document-root-is-pub DOCUMENT-ROOT-IS-PUB] [--magento-init-params MAGENTO-INIT-PARAMS]                                                                  |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--enable-debug-logging`:anchor--931:

디버그 로깅 활성화

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--enable-syslog-logging`:anchor--932:

syslog 로깅 활성화

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--backend-frontname`:anchor--933:

백엔드 프론트이름(누락된 경우 자동 생성됨)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--id_salt`:anchor--934:

GraphQl Salt

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-driver`:anchor--935:

원격 스토리지 드라이버

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-prefix`:anchor--936:

원격 저장소 접두사

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--remote-storage-endpoint`:anchor--937:

원격 저장소 끝점

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-bucket`:anchor--938:

원격 저장소 버킷

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-region`:anchor--939:

원격 스토리지 영역

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-key`:anchor--940:

원격 저장소 액세스 키

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--remote-storage-secret`:anchor--941:

원격 저장소 비밀 키

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--remote-storage-path-style`:anchor--942:

원격 스토리지 경로 스타일

+------------+
| List (ol)  |
+============+
| - 기본값: `0` |
| - 값 필요     |
+------------+

### `--amqp-host`:anchor--943:

Amqp 서버 호스트

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--amqp-port`:anchor--944:

Amqp 서버 포트

+---------------+
| List (ol)     |
+===============+
| - 기본값: `5672` |
| - 값 필요        |
+---------------+

### `--amqp-user`:anchor--945:

Amqp 서버 사용자 이름

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--amqp-password`:anchor--946:

Amqp 서버 암호

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--amqp-virtualhost`:anchor--947:

Amqp virtualhost

+------------+
| List (ol)  |
+============+
| - 기본값: `/` |
| - 값 필요     |
+------------+

### `--amqp-ssl`:anchor--948:

Amqp SSL

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--amqp-ssl-options`:anchor--949:

Amqp SSL 옵션(JSON)

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--consumers-wait-for-messages`:anchor--950:

소비자는 대기열에서 메시지를 기다려야 합니까? 1 - 예, 0 - 아니오

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--queue-default-connection`:anchor--951:

메시지 큐 기본 연결. 'db', 'amqp' 또는 사용자 지정 대기열 시스템일 수 있습니다. 대기열 시스템을 설치 및 구성해야 합니다. 그렇지 않으면 메시지가 올바르게 처리되지 않습니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--key`:anchor--952:

암호화 키

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-host`:anchor--953:

데이터베이스 서버 호스트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-name`:anchor--954:

데이터베이스 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-user`:anchor--955:

데이터베이스 서버 사용자 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-engine`:anchor--956:

데이터베이스 서버 엔진

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-password`:anchor--957:

데이터베이스 서버 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-prefix`:anchor--958:

데이터베이스 테이블 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-model`:anchor--959:

데이터베이스 유형

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-init-statements`:anchor--960:

데이터베이스 초기 명령 집합

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--skip-db-validation`, `-s`:anchor--961:

지정하면 DB 연결 유효성 검사를 건너뜁니다

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--http-cache-hosts`:anchor--962:

http 캐시 호스트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-ssl-key`:anchor--963:

SSL을 통해 DB 연결을 설정하기 위한 클라이언트 키 파일의 전체 경로

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--db-ssl-cert`:anchor--964:

SSL을 통해 DB 연결을 설정하기 위한 클라이언트 인증서 파일의 전체 경로

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--db-ssl-ca`:anchor--965:

SSL을 통해 DB 연결을 설정하기 위한 서버 인증서 파일의 전체 경로

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--db-ssl-verify`:anchor--966:

서버 인증 확인

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--session-save`:anchor--967:

세션 저장 핸들러

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-host`:anchor--968:

UNIX 소켓을 사용하는 경우 정규화된 호스트 이름, IP 주소 또는 절대 경로

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-port`:anchor--969:

Redis 서버 수신 포트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-password`:anchor--970:

Redis 서버 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-timeout`:anchor--971:

연결 시간 제한(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-persistent-id`:anchor--972:

영구 연결을 활성화하는 고유 문자열

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-db`:anchor--973:

Redis 데이터베이스 번호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-compression-threshold`:anchor--974:

Redis 압축 임계값

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-compression-lib`:anchor--975:

Redis 압축 라이브러리입니다. 값: gzip (기본값), lzf, lz4, snappy

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-log-level`:anchor--976:

Redis 로그 수준. 값: 0(최소 세부 정보) \~ 7(최대 세부 정보)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-max-concurrency`:anchor--977:

한 세션에 대한 잠금을 기다릴 수 있는 최대 프로세스 수

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-break-after-frontend`:anchor--978:

프론트엔드 세션에 대한 잠금을 해제하기 전에 대기할 시간(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-break-after-adminhtml`:anchor--979:

관리 세션에 대한 잠금을 해제하기 전에 대기할 시간(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-first-lifetime`:anchor--980:

첫 번째 쓰기 시 보트가 아닌 세션의 라이프타임(초)(비활성화하려면 0을 사용)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-bot-first-lifetime`:anchor--981:

첫 번째 쓰기 시 봇의 세션 수명(초)(비활성화하려면 0을 사용)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-bot-lifetime`:anchor--982:

후속 쓰기 시 봇에 대한 세션 수명(0을 사용하여 비활성화)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-disable-locking`:anchor--983:

잠금을 사용하지 않도록 설정합니다. 값: false(기본값), true

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-min-lifetime`:anchor--984:

Redis 최소 세션 수명(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-max-lifetime`:anchor--985:

Redis 최대 세션 수명(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-sentinel-master`:anchor--986:

레디스 센티넬 마스터

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-sentinel-servers`:anchor--987:

Redis Sentinel 서버, 쉼표로 구분

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-sentinel-verify-master`:anchor--988:

레디스 센티넬 검증 마스터 값: false(기본값), true

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-sentinel-connect-retries`:anchor--989:

Redis Sentinel 연결 다시 시도.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend`:anchor--990:

기본 캐시 처리기

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-server`:anchor--991:

Redis 서버

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-db`:anchor--992:

캐시에 대한 데이터베이스 번호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-port`:anchor--993:

Redis 서버 수신 포트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-password`:anchor--994:

Redis 서버 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-compress-data`:anchor--995:

압축을 비활성화하려면 0으로 설정합니다(기본값은 1, 활성화됨).

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-compression-lib`:anchor--996:

사용할 압축 라이브러리 \[snappy,lzf,l4z,zstd,gzip] (자동으로 결정하려면 비워 둡니다.)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-id-prefix`:anchor--997:

캐시 키의 ID 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--allow-parallel-generation`:anchor--998:

비차단 방식으로 캐시 생성 허용

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--page-cache`:anchor--999:

기본 캐시 처리기

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-server`:anchor--1000:

Redis 서버

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-db`:anchor--1001:

캐시에 대한 데이터베이스 번호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-port`:anchor--1002:

Redis 서버 수신 포트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-password`:anchor--1003:

Redis 서버 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-compress-data`:anchor--1004:

전체 페이지 캐시를 압축하려면 1로 설정합니다(비활성화하려면 0을 사용).

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-compression-lib`:anchor--1005:

사용할 압축 라이브러리 \[snappy,lzf,l4z,zstd,gzip] (자동으로 결정하려면 비워 둡니다.)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-id-prefix`:anchor--1006:

캐시 키의 ID 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-provider`:anchor--1007:

공급자 이름 잠금

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-db-prefix`:anchor--1008:

잠금 충돌을 방지하기 위한 설치별 잠금 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-zookeeper-host`:anchor--1009:

Zookeeper 클러스터에 연결할 호스트 및 포트입니다. 예: 127.0.0.1:2181

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-zookeeper-path`:anchor--1010:

Zookeeper가 잠금을 저장하는 경로입니다. 기본 경로는 /magento/locks입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-file-path`:anchor--1011:

파일 잠금이 저장되는 경로입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--document-root-is-pub`:anchor--1012:

Pub가 루트에 있고, true 또는 false만 표시할 수 있습니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--magento-init-params`:anchor--1013:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1014:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1015:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1016:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1017:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1018:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1019:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1020:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:db-data:upgrade`:anchor--1021:

DB에 데이터 설치 및 업그레이드

+-------------------------------------------------------------------------------+
| Code (language bash)                                                          |
+===============================================================================+
| ```                                                                           |
| bin/magento setup:db-data:upgrade [--magento-init-params MAGENTO-INIT-PARAMS] |
| ```                                                                           |
+-------------------------------------------------------------------------------+

### `--magento-init-params`:anchor--1022:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1023:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1024:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1025:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1026:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1027:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1028:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1029:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:db-declaration:generate-patch`:anchor--1030:

패치를 생성하여 특정 폴더에 넣습니다.

+-------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                              |
+===================================================================================================================+
| ```                                                                                                               |
| bin/magento setup:db-declaration:generate-patch [--revertable [REVERTABLE]] [--type [TYPE]] [--] <module> <patch> |
| ```                                                                                                               |
+-------------------------------------------------------------------------------------------------------------------+

### `module`:anchor--1031:

모듈 이름

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `patch`:anchor--1032:

패치 이름

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--revertable`:anchor--1033:

패치를 되돌릴 수 있는지 여부를 확인합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 허용합니다.    |
+----------------+

### `--type`:anchor--1034:

생성할 패치 유형을 확인하십시오. 사용 가능한 값: `data`, `schema`.

+---------------+
| List (ol)     |
+===============+
| - 기본값: `data` |
| - 값을 허용합니다.   |
+---------------+

### `--help`, `-h`:anchor--1035:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1036:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1037:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1038:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1039:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1040:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1041:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:db-declaration:generate-whitelist`:anchor--1042:

선언 설치 관리자에서 편집할 수 있는 테이블 및 열의 허용 목록 생성

+-----------------------------------------------------------------------------------+
| Code (language bash)                                                              |
+===================================================================================+
| ```                                                                               |
| bin/magento setup:db-declaration:generate-whitelist [--module-name [MODULE-NAME]] |
| ```                                                                               |
+-----------------------------------------------------------------------------------+

### `--module-name`:anchor--1043:

허용 목록이 생성될 모듈의 이름

+--------------+
| List (ol)    |
+==============+
| - 기본값: `all` |
| - 값을 허용합니다.  |
+--------------+

### `--help`, `-h`:anchor--1044:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1045:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1046:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1047:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1048:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1049:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1050:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:db-schema:upgrade`:anchor--1051:

DB 스키마 설치 및 업그레이드

+-------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                          |
+===============================================================================================================================+
| ```                                                                                                                           |
| bin/magento setup:db-schema:upgrade [--convert-old-scripts [CONVERT-OLD-SCRIPTS]] [--magento-init-params MAGENTO-INIT-PARAMS] |
| ```                                                                                                                           |
+-------------------------------------------------------------------------------------------------------------------------------+

### `--convert-old-scripts`:anchor--1052:

이전 스크립트(InstallSchema, UpgradeSchema)를 db\_schema.xml 형식으로 변환할 수 있습니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 허용합니다.    |
+----------------+

### `--magento-init-params`:anchor--1053:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1054:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1055:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1056:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1057:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1058:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1059:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1060:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:db:status`:anchor--1061:

DB 스키마 또는 데이터에 업그레이드가 필요한지 확인

+-------------------------------------------------------------------------+
| Code (language bash)                                                    |
+=========================================================================+
| ```                                                                     |
| bin/magento setup:db:status [--magento-init-params MAGENTO-INIT-PARAMS] |
| ```                                                                     |
+-------------------------------------------------------------------------+

### `--magento-init-params`:anchor--1062:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1063:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1064:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1065:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1066:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1067:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1068:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1069:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:di:compile`:anchor--1070:

자동 생성할 수 있는 DI 구성 및 누락된 모든 클래스를 생성합니다.

+------------------------------+
| Code (language bash)         |
+==============================+
| ```                          |
| bin/magento setup:di:compile |
| ```                          |
+------------------------------+

### `--help`, `-h`:anchor--1071:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1072:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1073:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1074:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1075:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1076:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1077:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:install`:anchor--1078:

Magento 애플리케이션 설치

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento setup:install [--enable-debug-logging ENABLE-DEBUG-LOGGING] [--enable-syslog-logging ENABLE-SYSLOG-LOGGING] [--backend-frontname BACKEND-FR |
| ONTNAME] [--id_salt ID_SALT] [--remote-storage-driver REMOTE-STORAGE-DRIVER] [--remote-storage-prefix REMOTE-STORAGE-PREFIX] [--remote-storage-endpoint |
|  REMOTE-STORAGE-ENDPOINT] [--remote-storage-bucket REMOTE-STORAGE-BUCKET] [--remote-storage-region REMOTE-STORAGE-REGION] [--remote-storage-key REMOTE- |
| STORAGE-KEY] [--remote-storage-secret REMOTE-STORAGE-SECRET] [--remote-storage-path-style REMOTE-STORAGE-PATH-STYLE] [--amqp-host AMQP-HOST] [--amqp-po |
| rt AMQP-PORT] [--amqp-user AMQP-USER] [--amqp-password AMQP-PASSWORD] [--amqp-virtualhost AMQP-VIRTUALHOST] [--amqp-ssl AMQP-SSL] [--amqp-ssl-options A |
| MQP-SSL-OPTIONS] [--consumers-wait-for-messages CONSUMERS-WAIT-FOR-MESSAGES] [--queue-default-connection QUEUE-DEFAULT-CONNECTION] [--key KEY] [--db-ho |
| st DB-HOST] [--db-name DB-NAME] [--db-user DB-USER] [--db-engine DB-ENGINE] [--db-password DB-PASSWORD] [--db-prefix DB-PREFIX] [--db-model DB-MODEL] [ |
| --db-init-statements DB-INIT-STATEMENTS] [-s\|--skip-db-validation] [--http-cache-hosts HTTP-CACHE-HOSTS] [--db-ssl-key DB-SSL-KEY] [--db-ssl-cert DB-S |
| SL-CERT] [--db-ssl-ca DB-SSL-CA] [--db-ssl-verify] [--session-save SESSION-SAVE] [--session-save-redis-host SESSION-SAVE-REDIS-HOST] [--session-save-re |
| dis-port SESSION-SAVE-REDIS-PORT] [--session-save-redis-password SESSION-SAVE-REDIS-PASSWORD] [--session-save-redis-timeout SESSION-SAVE-REDIS-TIMEOUT] |
|  [--session-save-redis-persistent-id SESSION-SAVE-REDIS-PERSISTENT-ID] [--session-save-redis-db SESSION-SAVE-REDIS-DB] [--session-save-redis-compressio |
| n-threshold SESSION-SAVE-REDIS-COMPRESSION-THRESHOLD] [--session-save-redis-compression-lib SESSION-SAVE-REDIS-COMPRESSION-LIB] [--session-save-redis-l |
| og-level SESSION-SAVE-REDIS-LOG-LEVEL] [--session-save-redis-max-concurrency SESSION-SAVE-REDIS-MAX-CONCURRENCY] [--session-save-redis-break-after-fron |
| tend SESSION-SAVE-REDIS-BREAK-AFTER-FRONTEND] [--session-save-redis-break-after-adminhtml SESSION-SAVE-REDIS-BREAK-AFTER-ADMINHTML] [--session-save-red |
| is-first-lifetime SESSION-SAVE-REDIS-FIRST-LIFETIME] [--session-save-redis-bot-first-lifetime SESSION-SAVE-REDIS-BOT-FIRST-LIFETIME] [--session-save-re |
| dis-bot-lifetime SESSION-SAVE-REDIS-BOT-LIFETIME] [--session-save-redis-disable-locking SESSION-SAVE-REDIS-DISABLE-LOCKING] [--session-save-redis-min-l |
| ifetime SESSION-SAVE-REDIS-MIN-LIFETIME] [--session-save-redis-max-lifetime SESSION-SAVE-REDIS-MAX-LIFETIME] [--session-save-redis-sentinel-master SESS |
| ION-SAVE-REDIS-SENTINEL-MASTER] [--session-save-redis-sentinel-servers SESSION-SAVE-REDIS-SENTINEL-SERVERS] [--session-save-redis-sentinel-verify-maste |
| r SESSION-SAVE-REDIS-SENTINEL-VERIFY-MASTER] [--session-save-redis-sentinel-connect-retries SESSION-SAVE-REDIS-SENTINEL-CONNECT-RETRIES] [--cache-backe |
| nd CACHE-BACKEND] [--cache-backend-redis-server CACHE-BACKEND-REDIS-SERVER] [--cache-backend-redis-db CACHE-BACKEND-REDIS-DB] [--cache-backend-redis-po |
| rt CACHE-BACKEND-REDIS-PORT] [--cache-backend-redis-password CACHE-BACKEND-REDIS-PASSWORD] [--cache-backend-redis-compress-data CACHE-BACKEND-REDIS-COM |
| PRESS-DATA] [--cache-backend-redis-compression-lib CACHE-BACKEND-REDIS-COMPRESSION-LIB] [--cache-id-prefix CACHE-ID-PREFIX] [--allow-parallel-generatio |
| n] [--page-cache PAGE-CACHE] [--page-cache-redis-server PAGE-CACHE-REDIS-SERVER] [--page-cache-redis-db PAGE-CACHE-REDIS-DB] [--page-cache-redis-port P |
| AGE-CACHE-REDIS-PORT] [--page-cache-redis-password PAGE-CACHE-REDIS-PASSWORD] [--page-cache-redis-compress-data PAGE-CACHE-REDIS-COMPRESS-DATA] [--page |
| -cache-redis-compression-lib PAGE-CACHE-REDIS-COMPRESSION-LIB] [--page-cache-id-prefix PAGE-CACHE-ID-PREFIX] [--lock-provider LOCK-PROVIDER] [--lock-db |
| -prefix LOCK-DB-PREFIX] [--lock-zookeeper-host LOCK-ZOOKEEPER-HOST] [--lock-zookeeper-path LOCK-ZOOKEEPER-PATH] [--lock-file-path LOCK-FILE-PATH] [--do |
| cument-root-is-pub DOCUMENT-ROOT-IS-PUB] [--base-url BASE-URL] [--language LANGUAGE] [--timezone TIMEZONE] [--currency CURRENCY] [--use-rewrites USE-RE |
| WRITES] [--use-secure USE-SECURE] [--base-url-secure BASE-URL-SECURE] [--use-secure-admin USE-SECURE-ADMIN] [--admin-use-security-key ADMIN-USE-SECURIT |
| Y-KEY] [--admin-user [ADMIN-USER]] [--admin-password [ADMIN-PASSWORD]] [--admin-email [ADMIN-EMAIL]] [--admin-firstname [ADMIN-FIRSTNAME]] [--admin-las |
| tname [ADMIN-LASTNAME]] [--search-engine SEARCH-ENGINE] [--elasticsearch-host ELASTICSEARCH-HOST] [--elasticsearch-port ELASTICSEARCH-PORT] [--elastics |
| earch-enable-auth ELASTICSEARCH-ENABLE-AUTH] [--elasticsearch-username ELASTICSEARCH-USERNAME] [--elasticsearch-password ELASTICSEARCH-PASSWORD] [--ela |
| sticsearch-index-prefix ELASTICSEARCH-INDEX-PREFIX] [--elasticsearch-timeout ELASTICSEARCH-TIMEOUT] [--opensearch-host OPENSEARCH-HOST] [--opensearch-p |
| ort OPENSEARCH-PORT] [--opensearch-enable-auth OPENSEARCH-ENABLE-AUTH] [--opensearch-username OPENSEARCH-USERNAME] [--opensearch-password OPENSEARCH-PA |
| SSWORD] [--opensearch-index-prefix OPENSEARCH-INDEX-PREFIX] [--opensearch-timeout OPENSEARCH-TIMEOUT] [--cleanup-database] [--sales-order-increment-pre |
| fix SALES-ORDER-INCREMENT-PREFIX] [--use-sample-data] [--enable-modules [ENABLE-MODULES]] [--disable-modules [DISABLE-MODULES]] [--convert-old-scripts  |
| [CONVERT-OLD-SCRIPTS]] [-i\|--interactive] [--safe-mode [SAFE-MODE]] [--data-restore [DATA-RESTORE]] [--dry-run [DRY-RUN]] [--magento-init-params MAGEN |
| TO-INIT-PARAMS]                                                                                                                                          |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--enable-debug-logging`:anchor--1079:

디버그 로깅 활성화

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--enable-syslog-logging`:anchor--1080:

syslog 로깅 활성화

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--backend-frontname`:anchor--1081:

백엔드 프론트이름(누락된 경우 자동 생성됨)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--id_salt`:anchor--1082:

GraphQl Salt

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-driver`:anchor--1083:

원격 스토리지 드라이버

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-prefix`:anchor--1084:

원격 저장소 접두사

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--remote-storage-endpoint`:anchor--1085:

원격 저장소 끝점

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-bucket`:anchor--1086:

원격 저장소 버킷

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-region`:anchor--1087:

원격 스토리지 영역

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--remote-storage-key`:anchor--1088:

원격 저장소 액세스 키

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--remote-storage-secret`:anchor--1089:

원격 저장소 비밀 키

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--remote-storage-path-style`:anchor--1090:

원격 스토리지 경로 스타일

+------------+
| List (ol)  |
+============+
| - 기본값: `0` |
| - 값 필요     |
+------------+

### `--amqp-host`:anchor--1091:

Amqp 서버 호스트

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--amqp-port`:anchor--1092:

Amqp 서버 포트

+---------------+
| List (ol)     |
+===============+
| - 기본값: `5672` |
| - 값 필요        |
+---------------+

### `--amqp-user`:anchor--1093:

Amqp 서버 사용자 이름

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--amqp-password`:anchor--1094:

Amqp 서버 암호

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--amqp-virtualhost`:anchor--1095:

Amqp virtualhost

+------------+
| List (ol)  |
+============+
| - 기본값: `/` |
| - 값 필요     |
+------------+

### `--amqp-ssl`:anchor--1096:

Amqp SSL

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--amqp-ssl-options`:anchor--1097:

Amqp SSL 옵션(JSON)

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--consumers-wait-for-messages`:anchor--1098:

소비자는 대기열에서 메시지를 기다려야 합니까? 1 - 예, 0 - 아니오

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--queue-default-connection`:anchor--1099:

메시지 큐 기본 연결. 'db', 'amqp' 또는 사용자 지정 대기열 시스템일 수 있습니다. 대기열 시스템을 설치 및 구성해야 합니다. 그렇지 않으면 메시지가 올바르게 처리되지 않습니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--key`:anchor--1100:

암호화 키

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-host`:anchor--1101:

데이터베이스 서버 호스트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-name`:anchor--1102:

데이터베이스 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-user`:anchor--1103:

데이터베이스 서버 사용자 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-engine`:anchor--1104:

데이터베이스 서버 엔진

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-password`:anchor--1105:

데이터베이스 서버 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-prefix`:anchor--1106:

데이터베이스 테이블 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-model`:anchor--1107:

데이터베이스 유형

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-init-statements`:anchor--1108:

데이터베이스 초기 명령 집합

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--skip-db-validation`, `-s`:anchor--1109:

지정하면 DB 연결 유효성 검사를 건너뜁니다

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--http-cache-hosts`:anchor--1110:

http 캐시 호스트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-ssl-key`:anchor--1111:

SSL을 통해 DB 연결을 설정하기 위한 클라이언트 키 파일의 전체 경로

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--db-ssl-cert`:anchor--1112:

SSL을 통해 DB 연결을 설정하기 위한 클라이언트 인증서 파일의 전체 경로

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--db-ssl-ca`:anchor--1113:

SSL을 통해 DB 연결을 설정하기 위한 서버 인증서 파일의 전체 경로

+-----------+
| List (ol) |
+===========+
| - 기본값: "  |
| - 값 필요    |
+-----------+

### `--db-ssl-verify`:anchor--1114:

서버 인증 확인

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--session-save`:anchor--1115:

세션 저장 핸들러

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-host`:anchor--1116:

UNIX 소켓을 사용하는 경우 정규화된 호스트 이름, IP 주소 또는 절대 경로

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-port`:anchor--1117:

Redis 서버 수신 포트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-password`:anchor--1118:

Redis 서버 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-timeout`:anchor--1119:

연결 시간 제한(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-persistent-id`:anchor--1120:

영구 연결을 활성화하는 고유 문자열

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-db`:anchor--1121:

Redis 데이터베이스 번호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-compression-threshold`:anchor--1122:

Redis 압축 임계값

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-compression-lib`:anchor--1123:

Redis 압축 라이브러리입니다. 값: gzip (기본값), lzf, lz4, snappy

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-log-level`:anchor--1124:

Redis 로그 수준. 값: 0(최소 세부 정보) \~ 7(최대 세부 정보)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-max-concurrency`:anchor--1125:

한 세션에 대한 잠금을 기다릴 수 있는 최대 프로세스 수

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-break-after-frontend`:anchor--1126:

프론트엔드 세션에 대한 잠금을 해제하기 전에 대기할 시간(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-break-after-adminhtml`:anchor--1127:

관리 세션에 대한 잠금을 해제하기 전에 대기할 시간(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-first-lifetime`:anchor--1128:

첫 번째 쓰기 시 보트가 아닌 세션의 라이프타임(초)(비활성화하려면 0을 사용)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-bot-first-lifetime`:anchor--1129:

첫 번째 쓰기 시 봇의 세션 수명(초)(비활성화하려면 0을 사용)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-bot-lifetime`:anchor--1130:

후속 쓰기 시 봇에 대한 세션 수명(0을 사용하여 비활성화)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-disable-locking`:anchor--1131:

잠금을 사용하지 않도록 설정합니다. 값: false(기본값), true

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-min-lifetime`:anchor--1132:

Redis 최소 세션 수명(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-max-lifetime`:anchor--1133:

Redis 최대 세션 수명(초)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-sentinel-master`:anchor--1134:

레디스 센티넬 마스터

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-sentinel-servers`:anchor--1135:

Redis Sentinel 서버, 쉼표로 구분

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-sentinel-verify-master`:anchor--1136:

레디스 센티넬 검증 마스터 값: false(기본값), true

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--session-save-redis-sentinel-connect-retries`:anchor--1137:

Redis Sentinel 연결 다시 시도.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend`:anchor--1138:

기본 캐시 처리기

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-server`:anchor--1139:

Redis 서버

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-db`:anchor--1140:

캐시에 대한 데이터베이스 번호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-port`:anchor--1141:

Redis 서버 수신 포트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-password`:anchor--1142:

Redis 서버 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-compress-data`:anchor--1143:

압축을 비활성화하려면 0으로 설정합니다(기본값은 1, 활성화됨).

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-backend-redis-compression-lib`:anchor--1144:

사용할 압축 라이브러리 \[snappy,lzf,l4z,zstd,gzip] (자동으로 결정하려면 비워 둡니다.)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cache-id-prefix`:anchor--1145:

캐시 키의 ID 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--allow-parallel-generation`:anchor--1146:

비차단 방식으로 캐시 생성 허용

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--page-cache`:anchor--1147:

기본 캐시 처리기

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-server`:anchor--1148:

Redis 서버

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-db`:anchor--1149:

캐시에 대한 데이터베이스 번호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-port`:anchor--1150:

Redis 서버 수신 포트

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-password`:anchor--1151:

Redis 서버 암호

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-compress-data`:anchor--1152:

전체 페이지 캐시를 압축하려면 1로 설정합니다(비활성화하려면 0을 사용).

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-redis-compression-lib`:anchor--1153:

사용할 압축 라이브러리 \[snappy,lzf,l4z,zstd,gzip] (자동으로 결정하려면 비워 둡니다.)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--page-cache-id-prefix`:anchor--1154:

캐시 키의 ID 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-provider`:anchor--1155:

공급자 이름 잠금

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-db-prefix`:anchor--1156:

잠금 충돌을 방지하기 위한 설치별 잠금 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-zookeeper-host`:anchor--1157:

Zookeeper 클러스터에 연결할 호스트 및 포트입니다. 예: 127.0.0.1:2181

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-zookeeper-path`:anchor--1158:

Zookeeper가 잠금을 저장하는 경로입니다. 기본 경로는 /magento/locks입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--lock-file-path`:anchor--1159:

파일 잠금이 저장되는 경로입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--document-root-is-pub`:anchor--1160:

Pub가 루트에 있고, true 또는 false만 표시할 수 있습니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--base-url`:anchor--1161:

스토어를 사용할 수 있어야 하는 URL입니다. 더 이상 사용되지 않음, config:set을 경로 web/unsecure/base\_url과 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--language`:anchor--1162:

기본 언어 코드. 사용되지 않음, config:set을 path general/locale/code와 함께 사용합니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--timezone`:anchor--1163:

기본 시간대 코드. 사용하지 않음, config:set을 경로 일반/로케일/시간대로 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--currency`:anchor--1164:

기본 통화 코드. 더 이상 사용되지 않음, 경로 통화/옵션/기본, 통화/옵션/기본 및 통화/옵션/허용과 함께 config:set 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--use-rewrites`:anchor--1165:

재작성을 사용합니다. 지원 중단됨, config:set를 경로 web/seo/use\_rewrites와 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--use-secure`:anchor--1166:

보안 URL을 사용합니다. SSL을 사용할 수 있는 경우에만 이 옵션을 활성화합니다. 지원 중단됨, config:set를 path web/secure/use\_in\_frontend와 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--base-url-secure`:anchor--1167:

SSL 연결을 위한 기본 URL입니다. 더 이상 사용되지 않음, config:set을 경로 web/secure/base\_url과 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--use-secure-admin`:anchor--1168:

SSL을 사용하여 관리 인터페이스를 실행합니다. 지원 중단됨, config:set를 경로 web/secure/use\_in\_adminhtml과 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--admin-use-security-key`:anchor--1169:

Magento 관리 URL 및 양식에서 "보안 키" 기능을 사용할지 여부입니다. 사용되지 않음, config:set을 경로 admin/security/use\_form\_key와 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--admin-user`:anchor--1170:

관리 사용자

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--admin-password`:anchor--1171:

관리자 암호

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--admin-email`:anchor--1172:

책임자 전자 메일

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--admin-firstname`:anchor--1173:

관리자 이름

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--admin-lastname`:anchor--1174:

관리자 성

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--search-engine`:anchor--1175:

검색 엔진. 값: elasticsearch5, elasticsearch7, elasticsearch8, opensearch

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--elasticsearch-host`:anchor--1176:

Elasticsearch 서버 호스트입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--elasticsearch-port`:anchor--1177:

Elasticsearch 서버 포트입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--elasticsearch-enable-auth`:anchor--1178:

인증을 활성화하려면 1로 설정합니다. (기본값은 0, 비활성화됨)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--elasticsearch-username`:anchor--1179:

Elasticsearch 사용자 이름입니다. HTTP 인증이 활성화된 경우에만 적용 가능

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--elasticsearch-password`:anchor--1180:

Elasticsearch 암호입니다. HTTP 인증이 활성화된 경우에만 적용 가능

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--elasticsearch-index-prefix`:anchor--1181:

Elasticsearch 인덱스 접두사입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--elasticsearch-timeout`:anchor--1182:

Elasticsearch 서버 시간 제한.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--opensearch-host`:anchor--1183:

OpenSearch 서버 호스트입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--opensearch-port`:anchor--1184:

OpenSearch 서버 포트입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--opensearch-enable-auth`:anchor--1185:

인증을 활성화하려면 1로 설정합니다. (기본값은 0, 비활성화됨)

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--opensearch-username`:anchor--1186:

OpenSearch 사용자 이름입니다. HTTP 인증이 활성화된 경우에만 적용 가능

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--opensearch-password`:anchor--1187:

암호 열기. HTTP 인증이 활성화된 경우에만 적용 가능

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--opensearch-index-prefix`:anchor--1188:

OpenSearch 색인 접두사입니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--opensearch-timeout`:anchor--1189:

OpenSearch 서버 시간 초과.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--cleanup-database`:anchor--1190:

설치하기 전에 데이터베이스 정리

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--sales-order-increment-prefix`:anchor--1191:

판매 주문 번호 접두사

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--use-sample-data`:anchor--1192:

샘플 데이터 사용

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--enable-modules`:anchor--1193:

쉼표로 구분된 모듈 이름 목록입니다. 설치하는 동안 포함해야 합니다. 사용 가능한 매직 매개 변수 "all".

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--disable-modules`:anchor--1194:

쉼표로 구분된 모듈 이름 목록입니다. 설치하는 동안 피해야 합니다. 사용 가능한 매직 매개 변수 "all".

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--convert-old-scripts`:anchor--1195:

이전 스크립트(InstallSchema, UpgradeSchema)를 db\_schema.xml 형식으로 변환할 수 있습니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 허용합니다.    |
+----------------+

### `--interactive`, `-i`:anchor--1196:

대화형 Magento 설치

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--safe-mode`:anchor--1197:

열 제거와 같은 파괴적인 작업에서 덤프와 함께 Magento의 안전한 설치

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--data-restore`:anchor--1198:

덤프에서 제거된 데이터 복원

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--dry-run`:anchor--1199:

Magento 설치는 시험 실행 모드에서 실행됩니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 허용합니다.    |
+----------------+

### `--magento-init-params`:anchor--1200:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1201:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1202:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1203:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1204:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1205:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1206:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1207:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:performance:generate-fixtures`:anchor--1208:

고정장치 생성

+-------------------------------------------------------------------------------------+
| Code (language bash)                                                                |
+=====================================================================================+
| ```                                                                                 |
| bin/magento setup:performance:generate-fixtures [-s\|--skip-reindex] [--] <profile> |
| ```                                                                                 |
+-------------------------------------------------------------------------------------+

### `profile`:anchor--1209:

프로필 구성 파일 경로

+-----------+
| List (ol) |
+===========+
| - 필수      |
+-----------+

### `--skip-reindex`, `-s`:anchor--1210:

색인 재지정 건너뛰기

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--1211:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1212:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1213:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1214:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1215:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1216:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1217:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:rollback`:anchor--1218:

Magento 응용 프로그램 코드베이스, 미디어 및 데이터베이스를 롤백합니다

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento setup:rollback [-c\|--code-file CODE-FILE] [-m\|--media-file MEDIA-FILE] [-d\|--db-file DB-FILE] [--magento-init-params MAGENTO-INIT-PARAMS |
| ]                                                                                                                                                        |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--code-file`, `-c`:anchor--1219:

var/backups의 코드 백업 파일 기본 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--media-file`, `-m`:anchor--1220:

var/backups의 미디어 백업 파일 기본 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--db-file`, `-d`:anchor--1221:

Var/backups의 DB 백업 파일 기본 이름

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--magento-init-params`:anchor--1222:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1223:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1224:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1225:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1226:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1227:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1228:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1229:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:static-content:deploy`:anchor--1230:

정적 보기 파일 배포

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento setup:static-content:deploy [-f\|--force] [-s\|--strategy [STRATEGY]] [-a\|--area [AREA]] [--exclude-area [EXCLUDE-AREA]] [-t\|--theme [THE |
| ME]] [--exclude-theme [EXCLUDE-THEME]] [-l\|--language [LANGUAGE]] [--exclude-language [EXCLUDE-LANGUAGE]] [-j\|--jobs [JOBS]] [--max-execution-time [M |
| AX-EXECUTION-TIME]] [--symlink-locale] [--content-version CONTENT-VERSION] [--refresh-content-version-only] [--no-javascript] [--no-js-bundle] [--no-cs |
| s] [--no-less] [--no-images] [--no-fonts] [--no-html] [--no-misc] [--no-html-minify] [--no-parent] [--] [<languages>...]                                 |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `languages`:anchor--1231:

정적 보기 파일을 출력할 ISO-639 언어 코드의 공백으로 구분된 목록입니다.

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 배열        |
+-------------+

### `--force`, `-f`:anchor--1232:

모든 모드에서 파일을 배포합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--strategy`, `-s`:anchor--1233:

지정된 전략을 사용하여 파일을 배포합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `quick` |
| - 값을 허용합니다.    |
+----------------+

### `--area`, `-a`:anchor--1234:

지정된 영역에 대해서만 파일을 생성합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `all`   |
| - 여러 값을 허용합니다. |
+----------------+

### `--exclude-area`:anchor--1235:

지정된 영역에 대한 파일을 생성하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `none`  |
| - 여러 값을 허용합니다. |
+----------------+

### `--theme`, `-t`:anchor--1236:

지정된 테마에만 정적 보기 파일을 생성합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `all`   |
| - 여러 값을 허용합니다. |
+----------------+

### `--exclude-theme`:anchor--1237:

지정된 테마에 대한 파일을 생성하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `none`  |
| - 여러 값을 허용합니다. |
+----------------+

### `--language`, `-l`:anchor--1238:

지정된 언어에 대한 파일만 생성합니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `all`   |
| - 여러 값을 허용합니다. |
+----------------+

### `--exclude-language`:anchor--1239:

지정된 언어에 대한 파일을 생성하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `none`  |
| - 여러 값을 허용합니다. |
+----------------+

### `--jobs`, `-j`:anchor--1240:

지정된 작업 수를 사용하여 병렬 처리를 활성화합니다.

+-------------+
| List (ol)   |
+=============+
| - 기본값: `0`  |
| - 값을 허용합니다. |
+-------------+

### `--max-execution-time`:anchor--1241:

배포 정적 프로세스의 최대 예상 실행 시간(초)입니다.

+--------------+
| List (ol)    |
+==============+
| - 기본값: `900` |
| - 값을 허용합니다.  |
+--------------+

### `--symlink-locale`:anchor--1242:

배포를 위해 전달되지만 사용자 지정이 없는 해당 로케일의 파일에 대한 심볼릭 링크를 만듭니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--content-version`:anchor--1243:

정적 콘텐츠의 사용자 지정 버전은 정적 콘텐츠 버전이 동일하고 캐싱이 제대로 작동하도록 여러 노드에서 배포를 실행하는 경우 사용할 수 있습니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--refresh-content-version-only`:anchor--1244:

정적 콘텐츠 버전을 새로 고치는 것은 브라우저 캐시 및 CDN 캐시의 정적 콘텐츠를 새로 고치는 데만 사용할 수 있습니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-javascript`:anchor--1245:

JavaScript 파일을 배포하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-js-bundle`:anchor--1246:

JavaScript 번들 파일을 배포하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-css`:anchor--1247:

CSS 파일을 배포하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-less`:anchor--1248:

더 적은 수의 파일을 배포하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-images`:anchor--1249:

이미지를 배포하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-fonts`:anchor--1250:

글꼴 파일을 배포하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-html`:anchor--1251:

HTML 파일을 배포하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-misc`:anchor--1252:

다른 형식(.md, .jbf, .csv 등)의 파일은 배포하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-html-minify`:anchor--1253:

HTML 파일을 축소하지 마십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-parent`:anchor--1254:

상위 테마를 컴파일하지 마십시오. 빠른 및 표준 전략에서만 지원됩니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--1255:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1256:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1257:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1258:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1259:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1260:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1261:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:store-config:set`:anchor--1262:

저장소 구성을 설치합니다. 2.2.0 이후 더 이상 사용되지 않습니다. 대신 config:set 사용

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento setup:store-config:set [--base-url BASE-URL] [--language LANGUAGE] [--timezone TIMEZONE] [--currency CURRENCY] [--use-rewrites USE-REWRITES |
| ] [--use-secure USE-SECURE] [--base-url-secure BASE-URL-SECURE] [--use-secure-admin USE-SECURE-ADMIN] [--admin-use-security-key ADMIN-USE-SECURITY-KEY] |
|  [--magento-init-params MAGENTO-INIT-PARAMS]                                                                                                             |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--base-url`:anchor--1263:

스토어를 사용할 수 있어야 하는 URL입니다. 더 이상 사용되지 않음, config:set을 경로 web/unsecure/base\_url과 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--language`:anchor--1264:

기본 언어 코드. 사용되지 않음, config:set을 path general/locale/code와 함께 사용합니다.

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--timezone`:anchor--1265:

기본 시간대 코드. 사용하지 않음, config:set을 경로 일반/로케일/시간대로 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--currency`:anchor--1266:

기본 통화 코드. 더 이상 사용되지 않음, 경로 통화/옵션/기본, 통화/옵션/기본 및 통화/옵션/허용과 함께 config:set 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--use-rewrites`:anchor--1267:

재작성을 사용합니다. 지원 중단됨, config:set를 경로 web/seo/use\_rewrites와 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--use-secure`:anchor--1268:

보안 URL을 사용합니다. SSL을 사용할 수 있는 경우에만 이 옵션을 활성화합니다. 지원 중단됨, config:set를 path web/secure/use\_in\_frontend와 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--base-url-secure`:anchor--1269:

SSL 연결을 위한 기본 URL입니다. 더 이상 사용되지 않음, config:set을 경로 web/secure/base\_url과 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--use-secure-admin`:anchor--1270:

SSL을 사용하여 관리 인터페이스를 실행합니다. 지원 중단됨, config:set를 경로 web/secure/use\_in\_adminhtml과 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--admin-use-security-key`:anchor--1271:

Magento 관리 URL 및 양식에서 "보안 키" 기능을 사용할지 여부입니다. 사용되지 않음, config:set을 경로 admin/security/use\_form\_key와 함께 사용

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--magento-init-params`:anchor--1272:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1273:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1274:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1275:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1276:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1277:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1278:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1279:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:uninstall`:anchor--1280:

Magento 애플리케이션 제거

+-------------------------------------------------------------------------+
| Code (language bash)                                                    |
+=========================================================================+
| ```                                                                     |
| bin/magento setup:uninstall [--magento-init-params MAGENTO-INIT-PARAMS] |
| ```                                                                     |
+-------------------------------------------------------------------------+

### `--magento-init-params`:anchor--1281:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1282:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1283:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1284:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1285:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1286:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1287:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1288:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `setup:upgrade`:anchor--1289:

Magento 애플리케이션, DB 데이터 및 스키마 업그레이드

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento setup:upgrade [--keep-generated] [--convert-old-scripts [CONVERT-OLD-SCRIPTS]] [--safe-mode [SAFE-MODE]] [--data-restore [DATA-RESTORE]] [- |
| -dry-run [DRY-RUN]] [--magento-init-params MAGENTO-INIT-PARAMS]                                                                                          |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--keep-generated`:anchor--1290:

생성된 파일이 삭제되지 않도록 합니다. 프로덕션에 배포할 때를 제외하고 이 옵션을 사용하지 않습니다. 자세한 내용은 시스템 통합자 또는 관리자에게 문의하십시오.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--convert-old-scripts`:anchor--1291:

이전 스크립트(InstallSchema, UpgradeSchema)를 db\_schema.xml 형식으로 변환할 수 있습니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 허용합니다.    |
+----------------+

### `--safe-mode`:anchor--1292:

열 제거와 같은 파괴적인 작업에서 덤프와 함께 Magento의 안전한 설치

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--data-restore`:anchor--1293:

덤프에서 제거된 데이터 복원

+-------------+
| List (ol)   |
+=============+
| - 값을 허용합니다. |
+-------------+

### `--dry-run`:anchor--1294:

Magento 설치는 시험 실행 모드에서 실행됩니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 허용합니다.    |
+----------------+

### `--magento-init-params`:anchor--1295:

Magento 초기화 매개변수를 사용자 정의하는 명령에 추가합니다. 예: "MAGE\_MODE=developer\&MAGE\_DIRS\[기본]\[path]=/var/www/example.com\&MAGE\_DIRS\[캐시]\[path]=/var/tmp/cache"

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1296:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1297:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1298:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1299:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1300:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1301:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1302:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `store:list`:anchor--1303:

저장소 목록을 표시합니다.

+------------------------+
| Code (language bash)   |
+========================+
| ```                    |
| bin/magento store:list |
| ```                    |
+------------------------+

### `--help`, `-h`:anchor--1304:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1305:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1306:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1307:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1308:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1309:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1310:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `store:website:list`:anchor--1311:

웹 사이트 목록 표시

+--------------------------------+
| Code (language bash)           |
+================================+
| ```                            |
| bin/magento store:website:list |
| ```                            |
+--------------------------------+

### `--help`, `-h`:anchor--1312:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1313:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1314:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1315:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1316:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1317:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1318:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `theme:uninstall`:anchor--1319:

테마 제거

+------------------------------------------------------------------------------------------+
| Code (language bash)                                                                     |
+==========================================================================================+
| ```                                                                                      |
| bin/magento theme:uninstall [--backup-code] [-c\|--clear-static-content] [--] <theme>... |
| ```                                                                                      |
+------------------------------------------------------------------------------------------+

### `theme`:anchor--1320:

테마의 경로. 테마 경로는 영역/공급업체/이름인 전체 경로로 지정해야 합니다. 예: frontend/Magento/blank

+-------------+
| List (ol)   |
+=============+
| - 기본값: `[]` |
|             |
| - 필수        |
|             |
| - 배열        |
+-------------+

### `--backup-code`:anchor--1321:

코드 백업 수행(임시 파일 제외)

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--clear-static-content`, `-c`:anchor--1322:

생성된 정적 보기 파일을 지웁니다.

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--help`, `-h`:anchor--1323:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1324:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1325:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1326:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1327:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1328:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1329:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

## `varnish:vcl:generate`:anchor--1330:

Varnish VCL을 생성하여 명령줄에 반향

+----------------------------------------------------------------------------------------------------------------------------------------------------------+
| Code (language bash)                                                                                                                                     |
+==========================================================================================================================================================+
| ```                                                                                                                                                      |
| bin/magento varnish:vcl:generate [--access-list ACCESS-LIST] [--backend-host BACKEND-HOST] [--backend-port BACKEND-PORT] [--export-version EXPORT-VERSI |
| ON] [--grace-period GRACE-PERIOD] [--output-file OUTPUT-FILE]                                                                                            |
| ```                                                                                                                                                      |
+----------------------------------------------------------------------------------------------------------------------------------------------------------+

### `--access-list`:anchor--1331:

바니시를 제거할 수 있는 IP 액세스 목록

+--------------------+
| List (ol)          |
+====================+
| - 기본값: `localhost` |
| - 값 필요             |
+--------------------+

### `--backend-host`:anchor--1332:

웹 백엔드의 호스트

+--------------------+
| List (ol)          |
+====================+
| - 기본값: `localhost` |
| - 값 필요             |
+--------------------+

### `--backend-port`:anchor--1333:

웹 백엔드의 포트

+---------------+
| List (ol)     |
+===============+
| - 기본값: `8080` |
| - 값 필요        |
+---------------+

### `--export-version`:anchor--1334:

Varnish 파일의 버전

+------------+
| List (ol)  |
+============+
| - 기본값: `4` |
| - 값 필요     |
+------------+

### `--grace-period`:anchor--1335:

유예 기간(초)

+--------------+
| List (ol)    |
+==============+
| - 기본값: `300` |
| - 값 필요       |
+--------------+

### `--output-file`:anchor--1336:

vcl을 쓸 파일의 경로

+-----------+
| List (ol) |
+===========+
| - 값 필요    |
+-----------+

### `--help`, `-h`:anchor--1337:

해당 명령에 대한 도움말을 표시합니다. 명령을 지정하지 않으면 다음에 대한 도움말을 표시합니다.\<info>list\&lt;/info> 명령

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--quiet`, `-q`:anchor--1338:

메시지 출력 안 함

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--verbose`, `-v|-vv|-vvv`:anchor--1339:

메시지의 자세한 정도를 증가시킵니다. 일반 출력의 경우 1, 자세한 출력의 경우 2, 디버그의 경우 3

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--version`, `-V`:anchor--1340:

이 응용 프로그램 버전 표시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--ansi`:anchor--1341:

ANSI 출력 강제(또는 비활성화 —no-ansi)

+--------------+
| List (ol)    |
+==============+
| - 값을 수락하지 않음 |
+--------------+

### `--no-ansi`:anchor--1342:

"—ansi" 옵션 무시

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

### `--no-interaction`, `-n`:anchor--1343:

대화식 질문하지 않음

+----------------+
| List (ol)      |
+================+
| - 기본값: `false` |
| - 값을 수락하지 않음   |
+----------------+

---

+--------------------------------------+
| Toc                                  |
+======================================+
| e36f2e69-ee80-4da7-b6a9-7e1ef4c482f6 |
+--------------------------------------+

---

+-------------+
| Doc Actions |
+=============+
|             |
+-------------+

+----------+
| Mini Toc |
+==========+
|          |
+----------+

+---------------------------------------------------------------------------------------------------------------------------------------------+
| Metadata                                                                                                                                    |
+==========================+==================================================================================================================+
| id                       | recQ1ACvnA9w8jzvu                                                                                                |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| keywords                 |                                                                                                                  |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| breadcrumb-url           | /docs/commerce-operations/reference/commerce-on-premises.html                                                    |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| cloud                    | Experience Cloud                                                                                                 |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| coveo-content-type       | Documentation                                                                                                    |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| description              | Magento Open Source bin/magento 명령줄 도구에 사용할 수 있는 모든 명령, 인수 및 옵션에 대해 알아봅니다.                                       |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| english-title            | bin/magento (Magento Open Source)                                                                                |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| exl-id                   | 360b027b-e621-479c-9921-3f5a3b58d9ca                                                                             |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| feature                  | Configuration                                                                                                    |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| feature-set              | Commerce                                                                                                         |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| git-commit               | bd2972ee4822693b405ac80d0997ce52b6f32773                                                                         |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| git-commit-file          | 7a567d6439821c382cc706711975aafc5202b48e                                                                         |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| git-edit                 | https\://github.com/AdobeDocs/commerce-operations.ko-KR/tree/main/help/reference/magento-open-source.md          |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| git-filename             | /help/reference/magento-open-source.md                                                                           |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| git-issue                | https\://github.com/AdobeDocs/commerce-operations.ko-KR/issues/new                                               |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| git-repo                 | https\://github.com/AdobeDocs/commerce-operations.ko-KR                                                          |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| ht-degree                | 0%                                                                                                               |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| index                    | true                                                                                                             |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| landing-page-description | Magento Open Source bin/magento 명령줄 도구에 사용할 수 있는 모든 명령, 인수 및 옵션에 대해 알아봅니다.                                       |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| last-update              | Tue Jun 27 2023 00:00:00 GMT\+0000 (Coordinated Universal Time)                                                  |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| level                    | Experienced                                                                                                      |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| mini-toc-levels          | 1                                                                                                                |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| next-page                | /docs/commerce-operations/reference/magento-open-source-beta.html?lang=ko                                        |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| original-solution        | Commerce                                                                                                         |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| pipeline\_filename       | /help/reference/magento-open-source.md                                                                           |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| pipeline\_internal       | false                                                                                                            |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| prev-page                | /docs/commerce-operations/reference/commerce-on-premises-beta.html?lang=ko                                       |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| product                  | magento                                                                                                          |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| publish-url              | https\://experienceleague.adobe.com/docs/commerce-operations/reference/magento-open-source.html?lang=ko          |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| recommendations          | noDisplay, noCatalog                                                                                             |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| role                     | Admin,Developer                                                                                                  |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| showfeedback             | true                                                                                                             |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| solution                 | Commerce                                                                                                         |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| solution-title           | Commerce                                                                                                         |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| source-git-commit        | 3b8b9d88e72722e5c72ab94f7d3e61b78a2cb136                                                                         |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| source-wordcount         | 27                                                                                                               |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| title                    | bin/magento(Magento Open Source) \| Adobe Commerce                                                               |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| topic                    | Administration, Commerce, Development                                                                            |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| type                     | Documentation                                                                                                    |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| user-guide-description   | Adobe Commerce 및 Magento Open Source 명령줄 도구에 사용할 수 있는 모든 명령, 인수 및 옵션에 대해 알아봅니다.                                  |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| user-guide-title         | 명령줄 도구 참조                                                                                                        |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| workflow-type            | tm\+mt                                                                                                           |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| build-date               | Wed Feb 14 2024 19:36:09 GMT\+0000 (Coordinated Universal Time)                                                  |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
| breadcrumbs              | \[{"title":"설명서","url":"/docs/"},{"title":"Commerce","url":"/docs/commerce.md?lang=ko"},{"title":"명령줄 도구         |
|                          | 참조","url":"/docs/commerce-operations/reference/commerce-on-premises.html?lang=ko"},{"title":"bin/magento(Magento |
|                          | Open Source)","url":""}]                                                                                         |
+--------------------------+------------------------------------------------------------------------------------------------------------------+
